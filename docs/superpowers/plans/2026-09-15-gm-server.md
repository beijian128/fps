# GM 服务与机器人陪练实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增第六个服务角色 `gm`（Gin HTTP + Web 操作页），提供「给玩家发钱」与「往匹配队列塞机器人凑人数」两条运维通路，机器人全程不动。

**Architecture:** `gm` 以 pitaya Cluster 模式构建 backend app（不注册 handler / remote、不监听任何 pitaya 端口），只主动发后端 RPC；HTTP 腿用 Gin 起在 `-gmaddr`。入队语义留在 `match`、钱包语义留在 `logic`，`gm` 既不碰 `match:queue` 也不碰钱包 Hash。机器人是队列里的普通成员，靠 `bot:` uid 前缀在 `match`（跳过在线探活）与 `game`（无客户端槽位）两侧识别。

**Tech Stack:** Go 1.26、pitaya v3（内置，Cluster = etcd + NATS）、Redis、protobuf（`protoc --go_out`）、Gin v1.12.0（纯 Go）。

**Spec:** [docs/superpowers/specs/2026-09-15-gm-server-design.md](../specs/2026-09-15-gm-server-design.md)

## Global Constraints

- 所有 Go 命令的工作目录是 `C:\Users\zhubeijian\Desktop\Projects\fps\joltgo`（模块根，`module joltgo`）；文档路径相对仓库根。
- 提交信息用中文，前缀遵循仓库既有习惯（`feat:` / `test:` / `docs:` / `chore:`）。
- 测试命令一律带 `-count=1`（禁用测试缓存）。
- **不改玩家可见协议**：`game.proto` 既有字段的编号与语义一律不动，只新增 message；客户端一行不改。
- **不新增 cgo**：`gm` 与 `bot` 都是纯 Go；`physics/` 仍是唯一允许 `import "C"` 的包。
- **不往 `third_party/pitaya` 加业务**：该目录只允许既有的三处安全脱敏补丁。
- `gm` 的密钥缺失时**拒绝启动**；`match` / `logic` 的管理入口一律「拒绝带会话的调用 + 校验密钥」两道闸。
- 机器人 uid 前缀的唯一真相是 `joltgo/bot` 包；任何地方不得再手写 `"bot:"` 字面量。
- Gin 版本固定 `github.com/gin-gonic/gin v1.12.0`（已在模块缓存，走 `goproxy.cn`）。

---

## 文件结构

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `joltgo/bot/bot.go` | 新建 | 机器人 uid 的唯一真相：前缀、构造、识别 |
| `joltgo/bot/bot_test.go` | 新建 | 前缀与识别的边界 |
| `joltgo/game/protos/game.proto` | 修改 | 新增 `AddBotsMsg/AddBotsReply/GrantCoinsMsg/GrantCoinsReply` |
| `joltgo/game/protos/game.pb.go` | 重新生成 | 由 `protoc --go_out` 产出，不手改 |
| `joltgo/match/queue.go` | 修改 | 队列新增「加入 N 个机器人」的语义入口（键与脚本仍在 match） |
| `joltgo/match/auth.go` | 新建 | match 侧管理入口判定：拒绝带会话的调用 + 常数时间比对密钥 |
| `joltgo/match/addbots.go` | 新建 | `QueueBots` handler：生成 bot uid、入队、触发配对 |
| `joltgo/match/match.go` | 修改 | `startMatch` 区分机器人与真人、全机器人配对丢弃、`pushMatchStatus` 跳过机器人；`Component` 持有密钥 |
| `joltgo/game/instance.go` | 修改 | 实例内 `bots [sim.MaxPlayers]bool` 标记 + 播发/推送目标跳过 |
| `joltgo/game/component.go` | 修改 | `Create` 不为机器人 uid 建注册表条目 |
| `joltgo/logic/service.go` | 修改 | 新增 `GrantCoins(ctx, accountID, delta) (int64, error)` |
| `joltgo/logic/component.go` | 修改 | `GrantCoins` handler + 密钥校验；`Component` 持有密钥 |
| `joltgo/gm/server.go` | 新建 | Gin 路由、鉴权中间件、两个接口的编排（依赖窄接口，便于测试） |
| `joltgo/gm/rpc.go` | 新建 | gm → match / logic 的 RPC 客户端（实现两个窄接口） |
| `joltgo/gm/page.go` + `index.html` | 新建 | `go:embed` 的单页 HTML |
| `joltgo/gm/server_test.go` | 新建 | `httptest` + stub 服务验证路由、鉴权、参数解析、错误码 |
| `joltgo/main.go` | 修改 | 第六个角色 `gm` + `-gmaddr` / `-gmkey`（或 `GM_KEY`） |
| `joltgo/deploy/start-all.ps1` | 修改 | 第六个进程与 `gm.log` |
| `AGENTS.md`、`docs/*.md`、`joltgo/deploy/README.md` | 修改 | 角色、flag、前缀约定、HTTP 接口、route 契约 |

---

## Task 1: `bot` 包 —— 机器人身份的唯一真相

**Files:**
- Create: `joltgo/bot/bot.go`
- Test: `joltgo/bot/bot_test.go`

**Interfaces:**
- Consumes: 无（`bot` 不 import 本仓库任何包）
- Produces: `bot.Prefix`（`const Prefix = "bot:"`）、`bot.New(n string) string`、`bot.Is(uid string) bool`

- [ ] **Step 1: 写失败的测试**

```go
// joltgo/bot/bot_test.go
package bot

import "testing"

func TestNewAlwaysCarriesPrefix(t *testing.T) {
	if got := New("abc123"); got != "bot:abc123" {
		t.Fatalf("New = %q, want %q", got, "bot:abc123")
	}
	if !Is(New("abc123")) {
		t.Fatal("New 造出来的 uid 必须被 Is 认成机器人")
	}
}

func TestIsAcceptsOnlyBotPrefix(t *testing.T) {
	cases := []struct {
		uid  string
		want bool
	}{
		{"bot:1", true},
		{"bot:4EoTb9pQ", true},
		{"bot:", true},    // 空后缀也算机器人 uid：判据只看身份来源，不看内容
		{"", false},
		{"1", false},      // 真人的十进制 accountID
		{"10001", false},  // 同上
		{"bot", false},    // 少了分隔符
		{"bots:1", false}, // 前缀相似但不是
		{" bot:1", false}, // 不 TrimSpace
		{"BOT:1", false},  // 大小写敏感
		{"xbot:1", false}, // 不在开头
	}
	for _, c := range cases {
		if got := Is(c.uid); got != c.want {
			t.Errorf("Is(%q) = %v, want %v", c.uid, got, c.want)
		}
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
go test -count=1 ./bot
```

预期：编译失败，`undefined: New` / `undefined: Is` / `undefined: Prefix`。

- [ ] **Step 3: 写实现**

```go
// joltgo/bot/bot.go
// Package bot 是机器人身份的唯一真相。
//
// 机器人以「队列里的普通成员」的身份进对局：它没有客户端、没有 gate、没有会话，
// 唯一能把它和真人区分开的就是 uid 前缀。有两个服务必须对同一条约定达成一致：
//
//   - match：配对时跳过「读在线登记 + 请 gate 写会话数据」（机器人必然两者皆无，
//     照真人路径走会被 100% 当成掉线剔除）；
//   - game：建实例时认出「这个槽位没有客户端」，从而不推帧、不进注册表、不参与回局。
//
// 判据写两遍就是两份真相，改一处漏一处，所以它单独成包。本包不 import 任何
// 本仓库的包，也不含任何业务逻辑。
package bot

import "strings"

// Prefix 是机器人 uid 的前缀。真人的 uid 是纯十进制的 accountID（见 account 服务），
// 永远撞不上这个前缀，因此 Is 不会误判真人。
const Prefix = "bot:"

// New 用给定的 n 拼出一个机器人 uid。n 由调用方生成（match 里用 nuid），
// 本包不关心它怎么来，只保证前缀。uid 本身是不透明的：配对、推送、注册表
// 全都只把它当字符串用，因此这里不需要全局计数器。
func New(n string) string { return Prefix + n }

// Is 报告 uid 是否是机器人 uid。前缀必须出现在开头，大小写敏感。
func Is(uid string) bool { return strings.HasPrefix(uid, Prefix) }
```

- [ ] **Step 4: 跑测试确认通过**

```powershell
go test -count=1 ./bot
```

预期：`ok  joltgo/bot`。

- [ ] **Step 5: 提交**

```powershell
git add joltgo/bot
git commit -m "feat: 新增 bot 包，统一机器人 uid 前缀约定"
```

---

## Task 2: 新增 GM 相关 proto 消息并重新生成

**Files:**
- Modify: `joltgo/game/protos/game.proto`（追加到文件末尾，不动既有 message）
- Regenerate: `joltgo/game/protos/game.pb.go`

**Interfaces:**
- Consumes: 无
- Produces（Go 侧生成类型，后续任务按这里的字段名引用）:
  - `*protos.AddBotsMsg{Count int32, AdminKey string}`
  - `*protos.AddBotsReply{Ok bool, Enqueued int32, Reason string}`
  - `*protos.GrantCoinsMsg{AccountId string, Delta int64, AdminKey string}`
  - `*protos.GrantCoinsReply{Ok bool, Coins int64, Reason string}`

- [ ] **Step 1: 确认现有 proto 生成命令**

```powershell
Get-ChildItem joltgo\game\protos
Select-String -Path docs\BUILD.md -Pattern 'protoc' -Context 2,4
```

先看清楚仓库里既有的 `protoc --go_out` 用法。注意 `joltgo/gen-redis.ps1` 是**持久化模型**的生成器，不要拿它跑 `game.proto`；两者不能混用。若 `docs/BUILD.md` 里没有记录，就把本次实际用的命令补进去（Task 11 会一起提交）。

- [ ] **Step 2: 追加 proto 定义**

在 `joltgo/game/protos/game.proto` 末尾追加（不改动任何既有字段编号）：

```proto
// AddBotsMsg 是 gm → match 的 RPC（route "match.match.addbots"）：往匹配队列里塞 N 个机器人。
//
// admin_key 是共享密钥。这两条 route 会被 gate 按前缀路由到 match / logic，
// 客户端也能发到，所以服务端不能靠「route 是内部的」来假设调用方可信。
message AddBotsMsg {
  int32 count = 1;     // 期望入队的机器人数量，服务端另有上限
  string admin_key = 2;
}

// AddBotsReply 是 match.match.addbots 的应答。
message AddBotsReply {
  bool ok = 1;
  int32 enqueued = 2; // 实际入队数量
  string reason = 3;  // ok=false 时的原因码
}

// GrantCoinsMsg 是 gm → logic 的 RPC（route "logic.logic.grantcoins"）：
// 给指定账号加（delta>0）或扣（delta<0）金币。用户名 → accountID 的解析在 gm 侧完成。
message GrantCoinsMsg {
  string account_id = 1;
  int64 delta = 2;
  string admin_key = 3;
}

// GrantCoinsReply 是 logic.logic.grantcoins 的应答。
message GrantCoinsReply {
  bool ok = 1;
  int64 coins = 2;   // 变更后的余额（ok=true 时有效）
  string reason = 3;
}
```

- [ ] **Step 3: 重新生成 `game.pb.go`**

按 Step 1 确认的命令执行（示例，按仓库实际路径调整 `--go_out` 与 `-I`）：

```powershell
protoc --go_out=. --go_opt=paths=source_relative -I. game.proto
```

- [ ] **Step 4: 验证生成物可用**

```powershell
go build ./game/...
go vet ./game/protos
```

预期：编译通过；`game.pb.go` 里能看到 `AddBotsMsg` / `GrantCoinsMsg` 及其 `GetAdminKey()`。

- [ ] **Step 5: 提交**

```powershell
git add joltgo/game/protos
git commit -m "feat: 增加 GM 加机器人与发钱的 wire 消息"
```

---

## Task 3: `match` 的队列接口与入口鉴权

**Files:**
- Modify: `joltgo/match/queue.go`（追加一个导出方法，不动既有脚本与键名）
- Create: `joltgo/match/auth.go`
- Test: `joltgo/match/queue_bots_test.go`、`joltgo/match/auth_test.go`

**Interfaces:**
- Consumes: `bot.New` / `bot.Is`（Task 1）
- Produces:
  - `func (q *Queue) QueueBots(ctx context.Context, n int, newUID func() string) (int, error)`
  - `func isClientCall(ctx context.Context, app pitaya.Pitaya) bool`
  - `func adminKeyAllowed(secret string, provided string) bool`

- [ ] **Step 1: 写失败的测试（队列）**

```go
// joltgo/match/queue_bots_test.go
package match

import (
	"context"
	"fmt"
	"testing"

	"joltgo/bot"
)

func TestQueueBotsEnqueuesRequestedCount(t *testing.T) {
	q, _ := newTestQueue(t)
	ctx := context.Background()
	seq := 0
	next := func() string {
		seq++
		return bot.New(fmt.Sprintf("t%d", seq))
	}

	n, err := q.QueueBots(ctx, 3, next)
	if err != nil {
		t.Fatalf("QueueBots 报错: %v", err)
	}
	if n != 3 {
		t.Fatalf("入队 %d 个，期望 3", n)
	}
	if got := q.queueSize(ctx, t); got != 3 {
		t.Fatalf("队列 %d 人，期望 3", got)
	}

	members, err := q.rdb.ZRange(ctx, queueKey, 0, -1).Result()
	if err != nil {
		t.Fatal(err)
	}
	for _, uid := range members {
		if !bot.Is(uid) {
			t.Fatalf("队列里的机器人 uid %q 必须带 bot 前缀", uid)
		}
	}
}

func TestQueueBotsWithNonPositiveCountWritesNothing(t *testing.T) {
	q, _ := newTestQueue(t)
	ctx := context.Background()
	called := false
	next := func() string { called = true; return bot.New("x") }

	for _, n := range []int{0, -1} {
		got, err := q.QueueBots(ctx, n, next)
		if err != nil {
			t.Fatalf("QueueBots(%d) 报错: %v", n, err)
		}
		if got != 0 {
			t.Fatalf("QueueBots(%d) = %d，期望 0", n, got)
		}
	}
	if called {
		t.Fatal("count<=0 时不应该生成 uid")
	}
	if got := q.queueSize(ctx, t); got != 0 {
		t.Fatalf("队列 %d 人，期望 0", got)
	}
}
```

`newTestQueue`、`queueSize`、`queueKey`、`q.rdb` 都是 `match/queue_test.go` 里既有测试辅助，直接复用，不要新建第二套。

- [ ] **Step 2: 跑测试确认失败**

```powershell
go test -count=1 -run TestQueueBots ./match
```

预期：编译失败，`q.QueueBots undefined`。

- [ ] **Step 3: 实现 `QueueBots`**

追加到 `joltgo/match/queue.go` 末尾（并在 import 块加 `"log"`）：

```go
// QueueBots 把 n 个机器人加入队列，返回实际成功入队的数量。
//
// 机器人与真人走**同一条**队列、同一个 Lua 脚本：它是队列里的普通成员，配对、
// 去重、按时长排序的语义完全一致。uid 由 newUID 提供（调用方决定怎么生成），
// 本方法只负责「生成 + 入队 + 数成功了几个」。
//
// 单个入队失败只记日志、不中断：调用方（GM 页面）没有补偿机会，返回的数量必须
// 诚实 —— 说入队了 3 个实际只有 2 个，比直接报错更难排查。
// 入队时间戳仍由脚本里的 Redis 服务端时钟决定，机器人也不例外。
func (q *Queue) QueueBots(ctx context.Context, n int, newUID func() string) (int, error) {
	if n <= 0 || newUID == nil {
		return 0, nil
	}
	queued := 0
	for i := 0; i < n; i++ {
		uid := newUID()
		if err := q.Enqueue(ctx, uid); err != nil {
			log.Printf("match: enqueue bot %s failed: %v", uid, err)
			continue
		}
		queued++
	}
	return queued, nil
}
```

- [ ] **Step 4: 跑测试确认通过**

```powershell
go test -count=1 -run TestQueueBots ./match
```

预期：PASS。

- [ ] **Step 5: 写失败的测试（入口鉴权）**

```go
// joltgo/match/auth_test.go
package match

import (
	"context"
	"testing"
)

func TestAdminKeyAllowedRequiresExactMatch(t *testing.T) {
	cases := []struct {
		secret, provided string
		want             bool
	}{
		{"s3cret", "s3cret", true},
		{"s3cret", "s3cret ", false},
		{"s3cret", "S3CRET", false},
		{"s3cret", "", false},
		{"", "s3cret", false}, // 服务端没配密钥时一律拒绝（不是「放行」）
		{"", "", false},
	}
	for _, c := range cases {
		if got := adminKeyAllowed(c.secret, c.provided); got != c.want {
			t.Errorf("adminKeyAllowed(%q,%q) = %v, want %v", c.secret, c.provided, got, c.want)
		}
	}
}

func TestIsClientCallOnlyTrueWithSession(t *testing.T) {
	if isClientCall(context.Background(), &joinTestApp{}) {
		t.Fatal("没有会话的 ctx 应判定为后端调用")
	}
	app := &joinTestApp{sess: &fakeSession{uid: "7"}}
	if !isClientCall(context.Background(), app) {
		t.Fatal("带会话的 ctx 应判定为客户端调用")
	}
}
```

`joinTestApp` 与 `fakeSession` 已在 `match_test.go` / `pending_abandon_test.go` 里存在（嵌入式 `pitaya.Pitaya` stub 与 `session.Session` stub）。若那里的类型名或字段名不同，按实际调整这两个测试的构造，**不要新建第二套 stub**。

- [ ] **Step 6: 跑测试确认失败**

```powershell
go test -count=1 -run 'TestAdminKeyAllowed|TestIsClientCall' ./match
```

预期：编译失败，`undefined: adminKeyAllowed` / `undefined: isClientCall`。

- [ ] **Step 7: 实现入口鉴权**

```go
// joltgo/match/auth.go
package match

import (
	"context"
	"crypto/subtle"

	pitaya "github.com/topfreegames/pitaya/v3/pkg"
)

// 本文件是 match 侧管理入口的两道闸，只服务于 GM 指令（当前只有 addbots）。
//
// 为什么需要两道：
//
//  1. route 本身不是权限。gate 把 match.* 按前缀转给 match 节点，所以**客户端**
//     发出的 match.match.addbots 会真的到达这里（RPCType_Sys，ctx 里带 Remote 会话）；
//     而后端 gm 用 app.RPCTo 发的（RPCType_User）**不带会话**。判据是「有没有会话」。
//  2. 只有第 1 条不够：任何后端都能调这条 route。密钥是第二道闸，缺一不可。

// isClientCall 判断这次调用是否来自客户端（而非后端 RPC）。
//
// 与 game 包里的同名函数判据完全一致：客户端经 gate 转发时 pitaya 会在 ctx 里放一个
// Remote 会话，后端 RPCTo 不会。偏严是刻意的 —— 将来若有后端改走带会话的路径，
// 会在日志里被明确拒绝，而不是静默放过一个管理入口。
func isClientCall(ctx context.Context, app pitaya.Pitaya) bool {
	return app != nil && app.GetSessionFromCtx(ctx) != nil
}

// adminKeyAllowed 报告请求携带的管理密钥是否等于本节点配置的密钥。
//
// 空密钥一律拒绝：**没配密钥 = 不提供服务**，而不是「谁都能发指令」。
// 比对走常数时间（subtle.ConstantTimeCompare）—— 这里比的是运维密钥，
// 不该用 == 泄漏「前几个字符对不对」。
func adminKeyAllowed(secret string, provided string) bool {
	if secret == "" {
		return false
	}
	return subtle.ConstantTimeCompare([]byte(secret), []byte(provided)) == 1
}
```

- [ ] **Step 8: 跑测试确认通过，且既有测试保持绿**

```powershell
go test -count=1 ./match
```

预期：全绿（含既有 `queue_nx_test` / `cancel_test` / `pending_abandon_test`）。

- [ ] **Step 9: 提交**

```powershell
git add joltgo/match
git commit -m "feat: match 支持机器人入队与管理入口鉴权"
```

---

## Task 4: `match.match.addbots` handler

**Files:**
- Create: `joltgo/match/addbots.go`
- Modify: `joltgo/match/match.go`（`Component` 加 `secret` 字段、`New` 加参数、加 `UpdateSecret`）
- Test: `joltgo/match/addbots_test.go`

**Interfaces:**
- Consumes: `Queue.QueueBots`、`isClientCall`、`adminKeyAllowed`（Task 3）、`protos.AddBotsMsg` / `protos.AddBotsReply`（Task 2）
- Produces:
  - `func New(app pitaya.Pitaya, queue *Queue, onl *online.Store, secret string) *Component`
  - `func (c *Component) UpdateSecret(secret string)`
  - `func (c *Component) QueueBots(ctx context.Context, msg *protos.AddBotsMsg) (*protos.AddBotsReply, error)`
  - `const maxBotsPerRequest = 8`

- [ ] **Step 1: 写失败的测试**

```go
// joltgo/match/addbots_test.go
package match

import (
	"context"
	"testing"

	"github.com/alicebob/miniredis/v2"
	"github.com/redis/go-redis/v9"
	"joltgo/bot"
	"joltgo/game/protos"
	"joltgo/online"
)

// newBotsTestComponent 造一个只带真队列与真密钥的 match 组件。
// app 用既有测试里的嵌入式 stub；本任务的 handler 只需要「有没有会话」这一个能力。
func newBotsTestComponent(t *testing.T, app *joinTestApp) (*Component, *miniredis.Miniredis) {
	t.Helper()
	if app == nil {
		app = &joinTestApp{}
	}
	mr := miniredis.RunT(t)
	rdb := redis.NewClient(&redis.Options{Addr: mr.Addr()})
	t.Cleanup(func() { _ = rdb.Close() })
	return New(app, NewQueue(rdb), online.NewStore(rdb), "s3cret"), mr
}

func TestQueueBotsAddsRequestedBots(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	ctx := context.Background()

	reply, err := c.QueueBots(ctx, &protos.AddBotsMsg{Count: 2, AdminKey: "s3cret"})
	if err != nil {
		t.Fatalf("QueueBots 报错: %v", err)
	}
	if !reply.Ok || reply.Enqueued != 2 {
		t.Fatalf("reply=%+v，期望 ok 且入队 2 个", reply)
	}
	if got := c.queue.queueSize(ctx, t); got != 2 {
		t.Fatalf("队列 %d 人，期望 2", got)
	}
}

func TestQueueBotsRejectsClientCall(t *testing.T) {
	// 客户端经 gate 转发来的调用带会话；管理入口必须拒绝，且**不动队列**。
	c, _ := newBotsTestComponent(t, &joinTestApp{sess: &fakeSession{uid: "7"}})
	ctx := context.Background()

	reply, err := c.QueueBots(ctx, &protos.AddBotsMsg{Count: 2, AdminKey: "s3cret"})
	if err != nil {
		t.Fatalf("拒绝路径不应返回 error: %v", err)
	}
	if reply.Ok || reply.Reason != "forbidden" {
		t.Fatalf("reply=%+v，期望 forbidden", reply)
	}
	if got := c.queue.queueSize(ctx, t); got != 0 {
		t.Fatalf("被拒绝的调用不该动队列，实际 %d 人", got)
	}
}

func TestQueueBotsRejectsWrongKey(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	ctx := context.Background()

	reply, err := c.QueueBots(ctx, &protos.AddBotsMsg{Count: 2, AdminKey: "nope"})
	if err != nil {
		t.Fatalf("拒绝路径不应返回 error: %v", err)
	}
	if reply.Ok || reply.Reason != "forbidden" {
		t.Fatalf("reply=%+v，期望 forbidden", reply)
	}
	if got := c.queue.queueSize(ctx, t); got != 0 {
		t.Fatalf("被拒绝的调用不该动队列，实际 %d 人", got)
	}
}

func TestQueueBotsClampsCount(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	ctx := context.Background()

	reply, err := c.QueueBots(ctx, &protos.AddBotsMsg{Count: maxBotsPerRequest + 100, AdminKey: "s3cret"})
	if err != nil {
		t.Fatal(err)
	}
	if !reply.Ok || reply.Enqueued != maxBotsPerRequest {
		t.Fatalf("reply=%+v，期望截到 %d", reply, maxBotsPerRequest)
	}
	if got := c.queue.queueSize(ctx, t); got != maxBotsPerRequest {
		t.Fatalf("队列 %d 人，期望 %d", got, maxBotsPerRequest)
	}
}

func TestQueueBotsWithZeroCountDoesNothing(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	ctx := context.Background()

	reply, err := c.QueueBots(ctx, &protos.AddBotsMsg{Count: 0, AdminKey: "s3cret"})
	if err != nil {
		t.Fatal(err)
	}
	if !reply.Ok || reply.Enqueued != 0 {
		t.Fatalf("reply=%+v，期望 ok 且 0", reply)
	}
	if got := c.queue.queueSize(ctx, t); got != 0 {
		t.Fatalf("队列 %d 人，期望 0", got)
	}
}

func TestQueueBotsGeneratesUniqueBotUIDs(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	ctx := context.Background()

	if _, err := c.QueueBots(ctx, &protos.AddBotsMsg{Count: 4, AdminKey: "s3cret"}); err != nil {
		t.Fatal(err)
	}
	members, err := c.queue.rdb.ZRange(ctx, queueKey, 0, -1).Result()
	if err != nil {
		t.Fatal(err)
	}
	if len(members) != 4 {
		t.Fatalf("应有 4 个成员，得到 %d", len(members))
	}
	seen := map[string]bool{}
	for _, uid := range members {
		if !bot.Is(uid) {
			t.Fatalf("机器人 uid %q 必须带 bot 前缀", uid)
		}
		if seen[uid] {
			t.Fatalf("uid %q 重复：ZSET 会去重，4 个请求只入队 3 个", uid)
		}
		seen[uid] = true
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
go test -count=1 -run TestQueueBots ./match
```

预期：编译失败（`New` 参数个数不匹配、`c.QueueBots` 未定义）。

- [ ] **Step 3: 给 `Component` 加密钥**

在 `joltgo/match/match.go` 的 `Component` 里加字段，并改 `New`：

```go
// Component 是 match 服务的 pitaya 组件。
type Component struct {
	component.Base
	app    pitaya.Pitaya
	queue  *Queue
	online *online.Store

	// secret 是 GM 管理指令的共享密钥（来自 -gmkey）。为空 = 不提供管理入口，
	// 因此 adminKeyAllowed 对空密钥一律拒绝。启动时由 main.go 写入。
	//
	// 用 atomic.Value 而不是裸字段：它由 main.go 在 app.Start() 之前写入、
	// 由 RPC handler goroutine 读取，两者之间没有 happens-before（pitaya 的 RPC
	// 走 NATS 回调），裸字段在这里是数据竞争。
	secret atomic.Value // string
}

// UpdateSecret 更新管理密钥。
func (c *Component) UpdateSecret(secret string) { c.secret.Store(secret) }

// adminSecret 读当前密钥，未设置时返回空串（adminKeyAllowed 会因此拒绝一切请求）。
func (c *Component) adminSecret() string {
	v, _ := c.secret.Load().(string)
	return v
}

// New 构造 match 组件。secret 为空表示本节点不提供 GM 管理入口。
func New(app pitaya.Pitaya, queue *Queue, onl *online.Store, secret string) *Component {
	c := &Component{app: app, queue: queue, online: onl}
	c.UpdateSecret(secret)
	return c
}
```

- [ ] **Step 4: 实现 handler**

```go
// joltgo/match/addbots.go
package match

import (
	"context"
	"log"

	"github.com/nats-io/nuid"
	"joltgo/bot"
	"joltgo/game/protos"
)

// maxBotsPerRequest 是一次 GM 指令最多入队的机器人数。
//
// 有上限不是为了保护队列（ZSET 塞几千个也没事），而是为了让「页面填错数字」的后果
// 可控：每个机器人后面都可能变成一个真的对局实例（一个 Jolt 世界 + 一条 goroutine），
// 而这一步是不可撤销的。超过就截断，并把实际入队数量回给页面。
const maxBotsPerRequest = 8

// QueueBots 是远端 RPC handler（route "match.match.addbots"）：GM 请求往匹配队列里
// 塞 N 个机器人。
//
// 两道入口检查（见 auth.go）：拒绝带会话的调用（客户端经 gate 发来的）、
// 校验共享密钥（任何后端都能调这条 route）。
//
// 入队语义留在本包：gm 不碰 match:queue、也不构造 bot uid —— 队列的写入口只有
// Queue.Enqueue 这一条路径。
func (c *Component) QueueBots(ctx context.Context, msg *protos.AddBotsMsg) (*protos.AddBotsReply, error) {
	if isClientCall(ctx, c.app) {
		log.Printf("match: addbots from client rejected")
		return &protos.AddBotsReply{Ok: false, Reason: "forbidden"}, nil
	}
	if !adminKeyAllowed(c.adminSecret(), msg.GetAdminKey()) {
		log.Printf("match: addbots rejected: bad admin key")
		return &protos.AddBotsReply{Ok: false, Reason: "forbidden"}, nil
	}

	count := int(msg.GetCount())
	if count <= 0 {
		return &protos.AddBotsReply{Ok: true, Reason: "noop"}, nil
	}
	if count > maxBotsPerRequest {
		count = maxBotsPerRequest
	}

	// nuid 是本仓库已有的直接依赖（match 用它生成 matchId），一串单调随机串，
	// 天然不重复，因此不需要 Redis 计数器。
	queued, err := c.queue.QueueBots(ctx, count, func() string { return bot.New(nuid.New().Next()) })
	if err != nil {
		log.Printf("match: addbots failed: %v", err)
		return &protos.AddBotsReply{Ok: false, Reason: "internal"}, nil
	}
	log.Printf("match: enqueued %d bots (requested %d)", queued, count)

	// 不等下一秒的 ticker：机器人通常立刻就能跟一个正在排队的真人配上。
	c.tryMatch(ctx)
	return &protos.AddBotsReply{Ok: true, Enqueued: int32(queued)}, nil
}
```

- [ ] **Step 5: 跑测试确认通过**

```powershell
go test -count=1 -run TestQueueBots ./match
```

预期：全绿。

- [ ] **Step 6: 提交**

```powershell
git add joltgo/match
git commit -m "feat: match 接受 GM 的机器人入队指令"
```

---

## Task 5: `match` 的配对规则 —— 机器人不探活、全机器人丢弃、状态推送跳过

**Files:**
- Modify: `joltgo/match/match.go`（`tryMatch` / `startMatch` / `pushMatchStatus`）
- Test: `joltgo/match/bot_pairing_test.go`

**Interfaces:**
- Consumes: `bot.Is`（Task 1）、`c.queue` / `c.online`、`bindGameOn` / `pushMatched`（既有）
- Produces: `tryMatch` / `startMatch` 的机器人语义；`startMatch` 改为返回 `bool`；`Component.pushMatchStatus` 跳过机器人

- [ ] **Step 1: 写测试用的 stub（复用既有形状）**

```go
// joltgo/match/bot_pairing_test.go
package match

import (
	"context"
	"testing"

	"github.com/golang/protobuf/proto"
	"joltgo/bot"
	"joltgo/game/protos"
)

// botPairApp 记录 bindgame 与 game.create 两次 RPC 的入参，用来断言
// 「机器人没有被当成掉线真人剔除」与「槽位怎么分」。
//
// joinTestApp / pushRecord / singleGameServer 都是既有的测试形状
// （见 match_test.go 与 cancel_test.go）。若那里的字段名或构造方式不同，
// 按实际调整本测试，**不要新建第二套 stub**。
type botPairApp struct {
	joinTestApp
	servers  map[string]*cluster.Server
	bound    []*protos.BindGameMsg
	created  *protos.CreateGameMsg
	createTo string
}

func (a *botPairApp) GetServersByType(string) (map[string]*cluster.Server, error) {
	return a.servers, nil
}

func (a *botPairApp) RPCTo(_ context.Context, serverID, routeStr string, reply proto.Message, arg proto.Message) error {
	switch routeStr {
	case bindGameRoute:
		a.bound = append(a.bound, proto.Clone(arg).(*protos.BindGameMsg))
		proto.Merge(reply.(*protos.BindGameReply), &protos.BindGameReply{Found: true})
	case gameCreateRoute:
		a.createTo = serverID
		a.created = proto.Clone(arg).(*protos.CreateGameMsg)
		proto.Merge(reply.(*protos.CreateGameReply), &protos.CreateGameReply{Code: 0})
	}
	return nil
}

func (a *botPairApp) SendPushToUsers(route string, msg proto.Message, uids []string, frontend string) (bool, error) {
	a.pushed = append(a.pushed, pushRecord{route: route, msg: msg, uids: append([]string(nil), uids...)})
	return true, nil
}
```

需要新增的 import：`github.com/topfreegames/pitaya/v3/pkg/cluster`（`joinTestApp` 若已 import 就不重复）。

- [ ] **Step 2: 写失败的测试**

```go
func TestStartMatchKeepsBotAndBindsHuman(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	app := &botPairApp{servers: singleGameServer()}
	c.app = app
	ctx := context.Background()

	botUID := bot.New("b1")
	if err := c.queue.Enqueue(ctx, botUID); err != nil {
		t.Fatal(err)
	}
	if err := c.queue.Enqueue(ctx, "10001"); err != nil {
		t.Fatal(err)
	}

	c.tryMatch(ctx)

	if app.created == nil {
		t.Fatal("真人在场时应建局")
	}
	if len(app.created.Uids) != 2 {
		t.Fatalf("应有两个槽位，得到 %v", app.created.Uids)
	}
	if app.created.Uids[0] != botUID {
		t.Fatalf("机器人应保住先入队的槽位 0，得到 %v", app.created.Uids)
	}
	if app.created.Uids[1] != "10001" {
		t.Fatalf("真人应在槽位 1，得到 %v", app.created.Uids)
	}
	for _, m := range app.bound {
		if bot.Is(m.Uid) {
			t.Fatalf("机器人不该有 gate 会话数据（bindgame 收到 %q）", m.Uid)
		}
	}
}

func TestTryMatchDiscardsAllBotPair(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	app := &botPairApp{servers: singleGameServer()}
	c.app = app
	ctx := context.Background()

	if err := c.queue.Enqueue(ctx, bot.New("b1")); err != nil {
		t.Fatal(err)
	}
	if err := c.queue.Enqueue(ctx, bot.New("b2")); err != nil {
		t.Fatal(err)
	}

	c.tryMatch(ctx)

	if app.created != nil {
		t.Fatalf("全机器人配对不该建局，得到 %+v", app.created)
	}
	if got := c.queue.queueSize(ctx, t); got != 0 {
		t.Fatalf("被丢弃的一对不该放回队列，实际 %d 人", got)
	}
}

func TestTryMatchDiscardsBotPairAndPairsWaitingHumans(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	app := &botPairApp{servers: singleGameServer()}
	c.app = app
	ctx := context.Background()

	// 入队顺序即弹出顺序：两个机器人先弹，然后是两个真人。
	for _, uid := range []string{bot.New("b1"), bot.New("b2"), "10001", "10002"} {
		if err := c.queue.Enqueue(ctx, uid); err != nil {
			t.Fatal(err)
		}
	}

	c.tryMatch(ctx)

	if app.created == nil {
		t.Fatal("丢掉全机器人那一对之后，应继续把两个真人配上")
	}
	if app.created.Uids[0] != "10001" || app.created.Uids[1] != "10002" {
		t.Fatalf("应配成两个真人，得到 %v", app.created.Uids)
	}
	if got := c.queue.queueSize(ctx, t); got != 0 {
		t.Fatalf("队列应被排空，实际 %d 人", got)
	}
}

func TestPushMatchStatusSkipsBotsButCountsThem(t *testing.T) {
	c, _ := newBotsTestComponent(t, nil)
	app := &botPairApp{servers: singleGameServer()}
	c.app = app
	ctx := context.Background()

	if err := c.queue.Enqueue(ctx, bot.New("b1")); err != nil {
		t.Fatal(err)
	}
	if err := c.queue.Enqueue(ctx, "10001"); err != nil {
		t.Fatal(err)
	}

	c.pushMatchStatus(ctx)

	if len(app.pushed) != 1 {
		t.Fatalf("只该给真人推一次状态，实际 %d 次: %+v", len(app.pushed), app.pushed)
	}
	if app.pushed[0].uids[0] != "10001" {
		t.Fatalf("推送目标应是真人，得到 %v", app.pushed[0].uids)
	}
	status, ok := app.pushed[0].msg.(*protos.MatchStatus)
	if !ok {
		t.Fatalf("推送载荷类型不对: %T", app.pushed[0].msg)
	}
	if status.QueuedPlayers != 2 {
		t.Fatalf("排队人数应把机器人算进去（2），得到 %d", status.QueuedPlayers)
	}
}
```

- [ ] **Step 3: 跑测试确认失败**

```powershell
go test -count=1 -run 'TestStartMatchKeepsBot|TestTryMatchDiscards|TestPushMatchStatusSkipsBots' ./match
```

预期：FAIL —— 现在机器人会被 `online.Gate` 判成离线剔除（`app.created.Uids` 只剩真人一个槽位），`pushMatchStatus` 还会给机器人推一次。

- [ ] **Step 4: 改 `startMatch` 的探活循环**

把 `joltgo/match/match.go` 里 `startMatch` 的探活循环改成按槽位确定性推进，并让函数返回 `bool`：

```go
	matchID := nuid.New().Next()

	// 1) 定地址 + 探活。**槽位 = uid 在名单里的下标**，所以这里必须按位置推进，
	//    而不能像旧实现那样「活下来几个就排几个」—— 机器人的位置要在建局时就固定，
	//    否则机器人与真人的槽位会随探活结果漂移。
	//
	//    机器人走另一条路径：它没有 gate、没有会话、没有在线登记，照真人路径走会被
	//    100% 当成掉线剔除（这正是必须改的地方）。真人的探活仍然照旧 ——
	//    「排队者可能已经掉线」这条判断对真人一字未变。
	slots := make([]string, 0, len(uids))
	humanAlive := 0
	for _, uid := range uids {
		if bot.Is(uid) {
			slots = append(slots, uid)
			continue
		}
		gateID, err := c.online.Gate(ctx, uid)
		if err != nil {
			log.Printf("match: requeue %s from match %s: online lookup failed: %v", uid, matchID, err)
			c.requeue(ctx, uid)
			continue
		}
		if gateID == "" {
			log.Printf("match: dropping %s from match %s: offline", uid, matchID)
			continue
		}
		if err := c.bindGameOn(ctx, gateID, uid, matchID, target.ID, len(slots)); err != nil {
			if errors.Is(err, errPlayerGone) {
				log.Printf("match: dropping %s from match %s: offline", uid, matchID)
			} else {
				log.Printf("match: requeue %s from match %s: bind failed: %v", uid, matchID, err)
				c.requeue(ctx, uid)
			}
			continue
		}
		slots = append(slots, uid)
		humanAlive++
	}

	// 2) 全机器人配对直接丢弃。两个木头人对站没有意义：谁都不动、谁都不开枪，
	//    只能空转到 30 分钟空闲回收。这一对已经被原子弹出队列，**不放回** ——
	//    放回会让它们反复被弹出、每次都重置等待时间（与当初删除单人兜底时的取舍一致）。
	if humanAlive == 0 {
		log.Printf("match: discarding all-bot pair %v", uids)
		return false
	}
	if len(slots) == 0 {
		return true
	}
```

`len(slots)` 作为 `playerIdx` 传给 `bindGameOn`，它必须与最终 `CreateGameMsg.Uids` 的下标一致 —— 所以 `slots` 是**唯一**的名单来源，后续两步都从它派生。

- [ ] **Step 5: 改建局与推送两段（同样返回 `bool`）**

```go
	// 3) 建局。失败则回滚会话数据（**只对真人**：机器人没有会话可回滚，直接丢掉即可），
	//    否则客户端会拿着一个不存在的 game 节点去发 game.cmd。
	reply := &protos.CreateGameReply{}
	if err := c.app.RPCTo(ctx, target.ID, gameCreateRoute, reply, &protos.CreateGameMsg{
		MatchId: matchID,
		Uids:    slots,
	}); err != nil {
		log.Printf("match: create game on %s failed: %v", target.ID, err)
		for idx, uid := range slots {
			if bot.Is(uid) {
				continue
			}
			gateID, gateErr := c.online.Gate(ctx, uid)
			if gateErr != nil || gateID == "" {
				log.Printf("match: rollback for %s skipped: no online gate", uid)
				c.requeue(ctx, uid)
				continue
			}
			if err := c.bindGameOn(ctx, gateID, uid, "", "", idx); err != nil {
				if errors.Is(err, errPlayerGone) {
					log.Printf("match: rollback for %s skipped: already offline", uid)
					continue
				}
				log.Printf("match: rollback bind for %s failed: %v", uid, err)
			}
			c.requeue(ctx, uid)
		}
		return true
	}

	// 4) 推结果。机器人没有客户端，pushMatched 对它就是一次无效推送，省掉它让日志干净些。
	for slot, uid := range slots {
		if bot.Is(uid) {
			continue
		}
		c.pushMatched(uid, matchID, target.ID, slot)
	}
	log.Printf("match: started match %s on game %s with %d slots (%d humans)",
		matchID, target.ID, len(slots), humanAlive)
	return true
```

原实现回滚时用的是循环下标 `i` 与平行的 `gates[i]`；这里改成按 uid 重查 gate，避免维护第二个平行数组（回滚是罕见路径，多一次 Redis 读可以接受）。同时把「没有可用 game 节点 → 全部 requeue」那条分支也改成 `return true`（已处理完，不再继续弹队）。

- [ ] **Step 6: 让 `tryMatch` 在丢弃后立刻重试**

```go
// tryMatch 尝试配对：队列凑满 2 人即开局。多个 match 节点同时抢也只有一个
// 能弹出（Lua 原子），不需要选主。
//
// 丢掉全机器人那一对之后要**立刻再试一次**：队列里若还排着别的真人，不该白等
// 下一秒的 ticker（AddBots 收尾也会调这里）。循环必然收敛 —— 每一轮至少出队两人。
func (c *Component) tryMatch(ctx context.Context) {
	for {
		uids, err := c.queue.PopPair(ctx)
		if err != nil {
			log.Printf("match: pop pair failed: %v", err)
			return
		}
		if len(uids) == 0 {
			return
		}
		if c.startMatch(ctx, uids) {
			// 成功开局、或已把真人放回队列、或集群侧没节点：都交给下一轮 tick。
			return
		}
		// startMatch 返回 false 只有一种情形：这一对全是机器人，已丢弃。继续弹下一对。
	}
}
```

- [ ] **Step 7: `pushMatchStatus` 跳过机器人**

```go
	for _, entry := range entries {
		if bot.Is(entry.UID) {
			// 机器人不在任何 gate 上，推送必然失败 —— 每 tick 一条错误日志，
			// 队列里有几个机器人就刷几倍。人数仍算它（total 不过滤）：
			// 对等待中的真人来说，队列里有几个机器人确实是「排队人数」。
			continue
		}
		status := &protos.MatchStatus{
			QueuedPlayers: int32(total),
			WaitedSeconds: entry.WaitedSeconds,
		}
		if _, err := c.app.SendPushToUsers(statusRoute, status, []string{entry.UID}, "gate"); err != nil {
			log.Printf("match: push %s to %s failed: %v", statusRoute, entry.UID, err)
		}
	}
```

- [ ] **Step 8: 跑测试确认通过，既有匹配测试保持绿**

```powershell
go test -count=1 ./match
```

预期：全绿。若既有 `match_test.go` / `cancel_test.go` 因 `startMatch` 返回值或 `New` 的参数变化而编译失败，**只改调用点与构造**，不改断言语义。

- [ ] **Step 9: 提交**

```powershell
git add joltgo/match
git commit -m "feat: 机器人不参与探活，全机器人配对丢弃"
```

---

## Task 6: `game` 把机器人槽位当作「无客户端槽位」

**Files:**
- Modify: `joltgo/game/instance.go`（`Instance` 加 `bots` 数组、`NewInstance` 填充、`presentUIDs` / `broadcast` 跳过）
- Modify: `joltgo/game/component.go`（`Create` 跳过机器人 uid 的注册表写入）
- Test: `joltgo/game/bot_slot_test.go`

**Interfaces:**
- Consumes: `bot.Is`（Task 1）、`sim.MaxPlayers`
- Produces: `Instance.bots [sim.MaxPlayers]bool`（包内可见）、`Component.Create` 的机器人语义

- [ ] **Step 1: 写失败的测试**

```go
// joltgo/game/bot_slot_test.go
package game

import (
	"context"
	"testing"

	"joltgo/bot"
	"joltgo/game/protos"
	"joltgo/sim"
)

func TestNewInstanceMarksBotSlots(t *testing.T) {
	inst := NewInstance(nil, "m1", []string{bot.New("b1"), "10001"})
	if !inst.bots[0] {
		t.Fatal("槽位 0 是机器人，应被标记")
	}
	if inst.bots[1] {
		t.Fatal("槽位 1 是真人，不该被标记")
	}
}

func TestPresentUIDsSkipsBotsAndEmptySlots(t *testing.T) {
	inst := NewInstance(nil, "m1", []string{bot.New("b1"), "10001"})
	got := inst.presentUIDs()
	if len(got) != 1 || got[0] != "10001" {
		t.Fatalf("推送目标只该有真人，得到 %v", got)
	}

	empty := NewInstance(nil, "m2", []string{"10001"})
	empty.uids = make([]string, sim.MaxPlayers)
	empty.uids[0] = "10001"
	empty.bots[0] = bot.Is(empty.uids[0])
	empty.bots[1] = bot.Is(empty.uids[1])
	if got := empty.presentUIDs(); len(got) != 1 || got[0] != "10001" {
		t.Fatalf("空槽位不该进推送目标，得到 %v", got)
	}
}

func TestCreateSkipsBotRegistryEntries(t *testing.T) {
	comp := New(&fakeGameApp{})
	reply, err := comp.Create(context.Background(), &protos.CreateGameMsg{
		MatchId: "m-bots",
		Uids:    []string{bot.New("b1"), "10001"},
	})
	if err != nil || reply.Code != 0 {
		t.Fatalf("create: reply=%+v err=%v", reply, err)
	}
	defer comp.Shutdown()

	comp.mu.Lock()
	_, botInReg := comp.uidToInst[bot.New("b1")]
	idx, humanIdx := comp.uidToIndex["10001"]
	comp.mu.Unlock()

	if botInReg {
		t.Fatal("机器人不该进实例注册表")
	}
	if !humanIdx || idx != 1 {
		t.Fatalf("真人应登记在槽位 1，得到 idx=%d ok=%v", idx, humanIdx)
	}
}
```

`fakeGameApp` 用 `component_test.go` 里既有的 stub（嵌入式 `pitaya.Pitaya`）；若那里的名字不同就改用实际名字，**不要新建第二套**。`NewInstance(nil, ...)` 是允许的：它只存 `app`，不立刻使用，前两个测试只碰槽位标记。第三个测试会真的建一个 Jolt 世界，因此与既有 `component_test.go` 的 `Create` 用例一样，需要在 `build.ps1` 之后、`libjolt_c.dll` 与 `joltgo.exe` 同目录的条件下跑。

- [ ] **Step 2: 跑测试确认失败**

```powershell
go test -count=1 -run 'TestNewInstanceMarksBotSlots|TestPresentUIDs|TestCreateSkipsBot' ./game
```

预期：编译失败，`inst.bots undefined`。

- [ ] **Step 3: 实现槽位标记**

在 `joltgo/game/instance.go` 的 `Instance` 结构体里加字段（紧跟 `left` 之后）：

```go
	// bots 标记「这个槽位是机器人」。它与 left 是两件事：left 的语义是
	// 「本来有这个人，他中途放弃了」（logic 靠它区分「放弃」与「空槽位」），
	// 把机器人标成 left 会让结算日志出现「机器人在中途放弃」这种假话。
	//
	// 机器人的效果是：不收帧、不在推送目标里、不进实例注册表、不参与回局 ——
	// 也就是「没有客户端的槽位」。结算侧不需要特殊处理：它的 SlotResult.Uid
	// 本来就是空串，logic 的 RecordMatch 已经会跳过空槽位。
	bots [sim.MaxPlayers]bool
```

`NewInstance` 里填充（并把 `sim.New(physics.New())` 的结果存成局部变量后再返回同一个实例）：

```go
func NewInstance(app pitaya.Pitaya, matchID string, uids []string) *Instance {
	inst := &Instance{
		app:     app,
		sim:     sim.New(physics.New()),
		matchID: matchID,
		uids:    uids,
		cmds:    make(chan func(), 128),
		stop:    make(chan struct{}),
	}
	for slot, uid := range inst.uids {
		if slot < sim.MaxPlayers {
			inst.bots[slot] = bot.Is(uid)
		}
	}
	return inst
}
```

`presentUIDs` 跳过机器人：

```go
// presentUIDs 返回在座玩家的 uid（跳过空槽位、机器人槽位与已经放弃对局的槽位）。
func (i *Instance) presentUIDs() []string {
	out := make([]string, 0, len(i.uids))
	for slot, uid := range i.uids {
		if uid == "" || i.left[slot] || i.bots[slot] {
			continue
		}
		out = append(out, uid)
	}
	return out
}
```

- [ ] **Step 4: 改 `broadcast` 的收件人筛选**

```go
	for slot, uid := range i.uids {
		if uid == "" || i.left[slot] || i.bots[slot] {
			// 空槽位与「已放弃对局」的玩家都不收帧；机器人没有客户端，
			// 给它推帧既没人收，也会让 pitaya 每次多走一趟 NATS 用户频道。
			continue
		}
		if i.pendingFull[slot] {
			i.pendingFull[slot] = false
			fullUIDs = append(fullUIDs, uid)
		} else {
			deltaUIDs = append(deltaUIDs, uid)
		}
	}
```

- [ ] **Step 5: `Create` 不给机器人建注册表条目**

```go
	c.mu.Lock()
	c.instances[msg.MatchId] = inst
	for idx, uid := range msg.Uids {
		if bot.Is(uid) {
			// 机器人不是账号，也不会有 game.cmd / 回局查询。写进注册表只会让
			// game.rejoin 与 match.findInstance 的 fan-out 多一次无意义的往返。
			continue
		}
		c.uidToInst[uid] = inst
		c.uidToIndex[uid] = idx
	}
	c.mu.Unlock()
```

- [ ] **Step 6: 跑测试确认通过**

```powershell
go test -count=1 ./game
```

预期：全绿（含既有 `frame_test` / `leave_test` / `finish_match_test`）。

- [ ] **Step 7: 提交**

```powershell
git add joltgo/game
git commit -m "feat: game 把机器人槽位当作无客户端槽位"
```

---

## Task 7: `logic` 的发钱入口

**Files:**
- Modify: `joltgo/logic/service.go`（新增 `GrantCoins`、`Store` 加 `AccountExists`、新增两个 reason 常量）
- Modify: `joltgo/logic/component.go`（新增 handler + 密钥字段 + `adminKeyAllowed`）
- Modify: `joltgo/persist/player.go`（新增 `AccountExists` 实现）
- Test: `joltgo/logic/grant_coins_test.go`

**Interfaces:**
- Consumes: `persist.PlayerStore.AddCoins`、`persist.AccountKey`、`protos.GrantCoinsMsg` / `protos.GrantCoinsReply`
- Produces:
  - `func (s *Service) GrantCoins(ctx context.Context, accountID string, delta int64) (int64, error)`
  - `func NewComponentWithSecret(app pitaya.Pitaya, service *Service, secret string) *Component`
  - `func (c *Component) UpdateSecret(secret string)`、`func (c *Component) GrantCoins(ctx context.Context, msg *protos.GrantCoinsMsg) (*protos.GrantCoinsReply, error)`
  - `const ReasonAccountNotFound = "account_not_found"`、`const ReasonForbidden = "forbidden"`

- [ ] **Step 1: 写失败的测试（service 层）**

```go
// joltgo/logic/grant_coins_test.go
package logic

import (
	"context"
	"testing"
)

func TestGrantCoinsAddsToExistingWallet(t *testing.T) {
	env, _ := newLockedServiceTestEnv(t)
	ctx := context.Background()
	if err := env.svc.EnsureProfile(ctx, "7"); err != nil {
		t.Fatal(err)
	}

	coins, err := env.svc.GrantCoins(ctx, "7", 500)
	if err != nil {
		t.Fatalf("GrantCoins 报错: %v", err)
	}
	if coins != 1500 {
		t.Fatalf("新余额 = %d，期望 1500（初始 1000 + 500）", coins)
	}
}

func TestGrantCoinsAllowsNegativeDelta(t *testing.T) {
	env, _ := newLockedServiceTestEnv(t)
	ctx := context.Background()
	if err := env.svc.EnsureProfile(ctx, "7"); err != nil {
		t.Fatal(err)
	}

	coins, err := env.svc.GrantCoins(ctx, "7", -400)
	if err != nil {
		t.Fatal(err)
	}
	if coins != 600 {
		t.Fatalf("新余额 = %d，期望 600", coins)
	}
}

func TestGrantCoinsRejectsUnknownAccount(t *testing.T) {
	env, _ := newLockedServiceTestEnv(t)
	_, err := env.svc.GrantCoins(context.Background(), "999999", 500)
	if ReasonOf(err) != ReasonAccountNotFound {
		t.Fatalf("未知账号应报 %s，得到 %q", ReasonAccountNotFound, ReasonOf(err))
	}
}

func TestGrantCoinsRejectsBadAccountID(t *testing.T) {
	env, _ := newLockedServiceTestEnv(t)
	for _, id := range []string{"", "abc", "0"} {
		if _, err := env.svc.GrantCoins(context.Background(), id, 500); err == nil {
			t.Fatalf("accountID %q 应被拒绝", id)
		}
	}
}
```

`newLockedServiceTestEnv` 是 `logic/service_test.go` 里既有的测试环境。它内部 fake store 的账号存在性判据、以及 "7" 是否已建档，都以该文件为准：若既有环境里没有 `999999` 这个「确定不存在」的 ID，就换一个同文件里同样保证不存在的值，**不要改既有用例的断言**。

- [ ] **Step 2: 跑测试确认失败**

```powershell
go test -count=1 -run TestGrantCoins ./logic
```

预期：编译失败，`env.svc.GrantCoins undefined`。

- [ ] **Step 3: 实现 service 层**

在 `joltgo/logic/service.go` 加常量：

```go
	// ReasonAccountNotFound 表示目标账号不存在（GM 发钱时不再隐式建档）。
	ReasonAccountNotFound = "account_not_found"
	// ReasonForbidden 表示管理入口拒绝：调用方带了会话，或密钥不匹配。
	ReasonForbidden = "forbidden"
```

`Store` 接口加一行：

```go
	// AccountExists 报告该账号是否真实存在（GM 发钱的前置校验，不隐式建档）。
	AccountExists(context.Context, uint64) (bool, error)
```

```go
// GrantCoins 给账号加（delta>0）或扣（delta<0）金币，返回变更后的余额。
//
// 与 Purchase 的区别：发钱不涉及商品、背包与装备，**没有跨命令的读-改-写不变量**，
// 所以不需要账号级分布式锁（user:lock:<accountID>）与补偿路径 —— 那套锁是为
// 「扣钱包 + 写背包」这个多命令提交准备的。单条 HINCRBY 本身原子，加锁等于把原子
// 操作降级（见 AGENTS.md §3.10）。
//
// 目标账号不存在时报错而不是建档：给不存在的账号发钱应该是「地址写错了」，
// 而不是顺手造一个只有金币、没有账号行的孤儿钱包。
func (s *Service) GrantCoins(ctx context.Context, accountID string, delta int64) (int64, error) {
	id, err := parseAccountID(accountID)
	if err != nil {
		return 0, err
	}
	exists, err := s.store.AccountExists(ctx, id)
	if err != nil {
		return 0, err
	}
	if !exists {
		return 0, reason(ReasonAccountNotFound)
	}
	if err := s.store.AddCoins(ctx, id, delta); err != nil {
		return 0, err
	}
	wallet, ok, err := s.store.GetWallet(ctx, id)
	if err != nil {
		return 0, err
	}
	if !ok {
		// 账号存在却没有钱包行：AddCoins 的 HINCRBY 会顺带建出这个 Hash，
		// 正常路径到不了这里。防御性返回，让调用方知道「读不回结果」。
		return 0, reason(ReasonProfileMissing)
	}
	return wallet.Coins, nil
}
```

- [ ] **Step 4: 在 `persist` 里实现 `AccountExists`**

追加到 `joltgo/persist/player.go`（`AccountKey` 已在 `persist/store.go` 导出，redigo 也已是既有 import）：

```go
// AccountExists 报告账号 Hash 是否存在。只做 EXISTS，不读字段 ——
// logic 对账号数据是**只读**的，它对账号的唯一兴趣是「这个人真的存在吗」。
// 放在 PlayerStore 上是因为 logic 手里只有这一个池；生成物（player.redis.go）
// 与账号模型都不受影响。
func (s *PlayerStore) AccountExists(ctx context.Context, id uint64) (bool, error) {
	conn, err := s.pool.GetContext(ctx)
	if err != nil {
		return false, err
	}
	defer conn.Close()
	return redis.Bool(conn.Do("EXISTS", AccountKey(id)))
}
```

若 `PlayerStore` 的池类型或字段名与上面不同，按 `player.go` 里既有方法（如 `GetWallet`）的写法对齐 —— **照抄它拿连接的那三行**，不要新造一种模式。

- [ ] **Step 5: 跑测试确认通过**

```powershell
go test -count=1 ./logic ./persist
```

预期：全绿。若既有 fake store 因 `Store` 接口新增方法而编译失败，补一个最小实现（返回 `true`），不改既有断言。

- [ ] **Step 6: 写失败的测试（component 层）**

```go
func newGrantCoinsTestEnv(t *testing.T, uid string) (*Component, *serviceTestEnv) {
	t.Helper()
	env, _ := newLockedServiceTestEnv(t)
	if uid != "" {
		if err := env.svc.EnsureProfile(context.Background(), uid); err != nil {
			t.Fatal(err)
		}
	}
	if err := env.svc.EnsureProfile(context.Background(), "7"); err != nil {
		t.Fatal(err)
	}
	return NewComponentWithSecret(&fakeApp{sess: &fakeSession{uid: uid}}, env.svc, "s3cret"), env
}

func TestGrantCoinsHandlerRejectsClientCall(t *testing.T) {
	c, _ := newGrantCoinsTestEnv(t, "7")
	reply, err := c.GrantCoins(context.Background(), &protos.GrantCoinsMsg{
		AccountId: "7", Delta: 500, AdminKey: "s3cret",
	})
	if err != nil {
		t.Fatal(err)
	}
	if reply.Ok || reply.Reason != ReasonForbidden {
		t.Fatalf("带会话的调用必须被拒，得到 %+v", reply)
	}
}

func TestGrantCoinsHandlerRejectsWrongKey(t *testing.T) {
	c, env := newGrantCoinsTestEnv(t, "")
	reply, err := c.GrantCoins(context.Background(), &protos.GrantCoinsMsg{
		AccountId: "7", Delta: 500, AdminKey: "nope",
	})
	if err != nil {
		t.Fatal(err)
	}
	if reply.Ok || reply.Reason != ReasonForbidden {
		t.Fatalf("密钥错误必须被拒，得到 %+v", reply)
	}
	state, err := env.svc.State(context.Background(), "7")
	if err != nil {
		t.Fatal(err)
	}
	if state.Coins != 1000 {
		t.Fatalf("被拒的调用不该改钱包，余额 %d", state.Coins)
	}
}

func TestGrantCoinsHandlerGrants(t *testing.T) {
	c, env := newGrantCoinsTestEnv(t, "")
	reply, err := c.GrantCoins(context.Background(), &protos.GrantCoinsMsg{
		AccountId: "7", Delta: 500, AdminKey: "s3cret",
	})
	if err != nil {
		t.Fatal(err)
	}
	if !reply.Ok || reply.Coins != 1500 {
		t.Fatalf("reply=%+v，期望 ok 且 1500", reply)
	}
	state, err := env.svc.State(context.Background(), "7")
	if err != nil {
		t.Fatal(err)
	}
	if state.Coins != 1500 {
		t.Fatalf("钱包余额 = %d，期望 1500", state.Coins)
	}
}

func TestGrantCoinsHandlerReportsUnknownAccount(t *testing.T) {
	c, _ := newGrantCoinsTestEnv(t, "")
	reply, err := c.GrantCoins(context.Background(), &protos.GrantCoinsMsg{
		AccountId: "999999", Delta: 500, AdminKey: "s3cret",
	})
	if err != nil {
		t.Fatal(err)
	}
	if reply.Ok || reply.Reason != ReasonAccountNotFound {
		t.Fatalf("未知账号应回 %s，得到 %+v", ReasonAccountNotFound, reply)
	}
}
```

- [ ] **Step 7: 跑测试确认失败**

```powershell
go test -count=1 -run TestGrantCoinsHandler ./logic
```

预期：编译失败，`NewComponentWithSecret` / `Component.GrantCoins` 未定义。

- [ ] **Step 8: 实现 component 层**

在 `joltgo/logic/component.go` 里加字段、构造函数与 handler：

```go
	// secret 是 GM 管理指令的共享密钥（-gmkey）。为空 = 不提供管理入口，
	// 因此 adminKeyAllowed 对空密钥一律拒绝。
	secret atomic.Value // string
```

```go
// NewComponentWithSecret 构造带 GM 管理入口的组件。secret 为空表示不提供该入口。
func NewComponentWithSecret(app pitaya.Pitaya, service *Service, secret string) *Component {
	c := NewComponent(app, service)
	c.UpdateSecret(secret)
	return c
}

// UpdateSecret 更新管理密钥（原子写入，理由见 match 侧同名方法）。
func (c *Component) UpdateSecret(secret string) { c.secret.Store(secret) }

func (c *Component) adminSecret() string {
	v, _ := c.secret.Load().(string)
	return v
}

// adminKeyAllowed 报告请求携带的管理密钥是否等于本节点配置的密钥。
// 空密钥一律拒绝（没配密钥 = 不提供服务）；比对走常数时间。
func adminKeyAllowed(secret string, provided string) bool {
	if secret == "" {
		return false
	}
	return subtle.ConstantTimeCompare([]byte(secret), []byte(provided)) == 1
}

// GrantCoins 是远端 RPC handler（route "logic.logic.grantcoins"）：GM 请求给账号发钱。
//
// 两道入口检查与 match.QueueBots 相同：拒绝带会话的调用（客户端经 gate 发的
// logic.logic.grantcoins 会真的到这儿），再校验共享密钥。
func (c *Component) GrantCoins(ctx context.Context, msg *protos.GrantCoinsMsg) (*protos.GrantCoinsReply, error) {
	if s := c.app.GetSessionFromCtx(ctx); s != nil {
		log.Printf("logic: grantcoins from client rejected (uid=%s)", s.UID())
		return &protos.GrantCoinsReply{Ok: false, Reason: ReasonForbidden}, nil
	}
	if !adminKeyAllowed(c.adminSecret(), msg.GetAdminKey()) {
		log.Printf("logic: grantcoins rejected: bad admin key")
		return &protos.GrantCoinsReply{Ok: false, Reason: ReasonForbidden}, nil
	}

	coins, err := c.service.GrantCoins(ctx, msg.GetAccountId(), msg.GetDelta())
	if err != nil {
		return &protos.GrantCoinsReply{Ok: false, Reason: ReasonOf(err)}, nil
	}
	return &protos.GrantCoinsReply{Ok: true, Coins: coins}, nil
}
```

两个包各持有一份逐字相同的 `adminKeyAllowed`（12 行），不为它引入跨包依赖。后续 import 需要补 `crypto/subtle`、`log`、`sync/atomic`。

- [ ] **Step 9: 跑测试确认通过**

```powershell
go test -count=1 ./logic
```

预期：全绿。

- [ ] **Step 10: 提交**

```powershell
git add joltgo/logic joltgo/persist
git commit -m "feat: logic 提供 GM 发钱入口"
```

---

## Task 8: `gm` 服务 —— Gin 路由、鉴权与两个接口

**Files:**
- Create: `joltgo/gm/server.go`、`joltgo/gm/page.go`、`joltgo/gm/index.html`（本任务给最小占位，Task 9 换成真页面）
- Test: `joltgo/gm/server_test.go`

**Interfaces:**
- Consumes: `bot`（间接）、`persist.AccountStore`
- Produces:
  - `type CoinsService interface { GrantCoins(ctx context.Context, accountID string, delta int64) (int64, error) }`
  - `type AddBotsService interface { AddBots(ctx context.Context, count int32) (int32, error) }`
  - `func NewHandler(coins CoinsService, bots AddBotsService, accounts *persist.AccountStore, secret string) *Handler`
  - `func (h *Handler) Router() *gin.Engine`
  - `func (h *Handler) resolveAccountID(ctx context.Context, target string) (string, error)`

- [ ] **Step 1: 引入 Gin**

```powershell
go get github.com/gin-gonic/gin@v1.12.0
go mod tidy
```

预期：`go.mod` 出现 `github.com/gin-gonic/gin v1.12.0`，`go.sum` 更新。

- [ ] **Step 2: 写失败的测试**

```go
// joltgo/gm/server_test.go
package gm

import (
	"context"
	"encoding/json"
	"errors"
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"

	"github.com/alicebob/miniredis/v2"
	"github.com/gin-gonic/gin"
	"github.com/gomodule/redigo/redis"
	"joltgo/game/protos"
	"joltgo/persist"
)

type stubCoins struct {
	gotID    string
	gotDelta int64
	coins    int64
	err      error
}

func (s *stubCoins) GrantCoins(_ context.Context, accountID string, delta int64) (int64, error) {
	s.gotID, s.gotDelta = accountID, delta
	return s.coins, s.err
}

type stubBots struct {
	gotCount int32
	enqueued int32
	err      error
}

func (s *stubBots) AddBots(_ context.Context, count int32) (int32, error) {
	s.gotCount = count
	if s.err != nil {
		return 0, s.err
	}
	return s.enqueued, nil
}

func newTestHandler(t *testing.T) (*Handler, *stubCoins, *stubBots) {
	t.Helper()
	gin.SetMode(gin.TestMode)
	mr := miniredis.RunT(t)
	pool := newRedigoPool(t, mr.Addr())
	accounts := persist.NewAccountStore(pool)
	if err := accounts.Save(context.Background(), 10001, "alice", "hash", 1); err != nil {
		t.Fatal(err)
	}
	coins := &stubCoins{coins: 1500}
	bots := &stubBots{enqueued: 1}
	return NewHandler(coins, bots, accounts, "s3cret"), coins, bots
}

// newRedigoPool 造一个指向 miniredis 的 redigo 池。
// 若 logic / persist 的测试里已有等价辅助函数，直接复用它的写法（保持一种风格）。
func newRedigoPool(t *testing.T, addr string) *redis.Pool {
	t.Helper()
	pool := &redis.Pool{
		MaxIdle: 1,
		DialContext: func(context.Context) (redis.Conn, error) {
			return redis.Dial("tcp", addr)
		},
	}
	t.Cleanup(func() { _ = pool.Close() })
	return pool
}

func postJSON(t *testing.T, router *gin.Engine, path, body, key string) *httptest.ResponseRecorder {
	t.Helper()
	req := httptest.NewRequest(http.MethodPost, path, strings.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	if key != "" {
		req.Header.Set("Authorization", "Bearer "+key)
	}
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)
	return w
}
```

`redigo` 的 `DialContext` 签名按仓库里实际版本对齐（`redis.Dial("tcp", addr)` 的返回值形状见 `kv.OpenRedigo` 或既有测试）。

- [ ] **Step 3: 写测试主体**

```go
func TestIndexServesHTMLWithoutAuth(t *testing.T) {
	h, _, _ := newTestHandler(t)
	req := httptest.NewRequest(http.MethodGet, "/", nil)
	w := httptest.NewRecorder()
	h.Router().ServeHTTP(w, req)
	if w.Code != http.StatusOK {
		t.Fatalf("GET / = %d，期望 200", w.Code)
	}
	if !strings.Contains(w.Body.String(), "<html") {
		t.Fatal("GET / 应返回 HTML 页面")
	}
}

func TestCoinsRejectsMissingKey(t *testing.T) {
	h, coins, _ := newTestHandler(t)
	w := postJSON(t, h.Router(), "/api/coins", `{"target":"alice","delta":500}`, "")
	if w.Code != http.StatusUnauthorized {
		t.Fatalf("缺密钥 = %d，期望 401", w.Code)
	}
	if coins.gotID != "" {
		t.Fatal("鉴权失败的请求不该触达下游服务")
	}
}

func TestCoinsByUsername(t *testing.T) {
	h, coins, _ := newTestHandler(t)
	w := postJSON(t, h.Router(), "/api/coins", `{"target":"alice","delta":500}`, "s3cret")
	if w.Code != http.StatusOK {
		t.Fatalf("= %d，body=%s", w.Code, w.Body.String())
	}
	if coins.gotID != "10001" || coins.gotDelta != 500 {
		t.Fatalf("下游收到 id=%q delta=%d，期望 10001 / 500", coins.gotID, coins.gotDelta)
	}
	var body struct {
		Ok    bool  `json:"ok"`
		Coins int64 `json:"coins"`
	}
	if err := json.Unmarshal(w.Body.Bytes(), &body); err != nil {
		t.Fatal(err)
	}
	if !body.Ok || body.Coins != 1500 {
		t.Fatalf("body=%+v，期望 ok 且 1500", body)
	}
}

func TestCoinsByUsernameIsCaseInsensitive(t *testing.T) {
	h, coins, _ := newTestHandler(t)
	postJSON(t, h.Router(), "/api/coins", `{"target":"ALICE","delta":1}`, "s3cret")
	if coins.gotID != "10001" {
		t.Fatalf("大小写不敏感的用户名应解析到 10001，得到 %q", coins.gotID)
	}
}

func TestCoinsByAccountID(t *testing.T) {
	h, coins, _ := newTestHandler(t)
	w := postJSON(t, h.Router(), "/api/coins", `{"target":"10001","delta":500}`, "s3cret")
	if w.Code != http.StatusOK {
		t.Fatalf("= %d，body=%s", w.Code, w.Body.String())
	}
	if coins.gotID != "10001" {
		t.Fatalf("纯十进制应直接当 accountID，得到 %q", coins.gotID)
	}
}

func TestCoinsUnknownUsername(t *testing.T) {
	h, coins, _ := newTestHandler(t)
	w := postJSON(t, h.Router(), "/api/coins", `{"target":"nobody","delta":1}`, "s3cret")
	if w.Code != http.StatusNotFound {
		t.Fatalf("未知用户名 = %d，期望 404", w.Code)
	}
	if coins.gotID != "" {
		t.Fatal("解析失败不该调下游")
	}
}

func TestCoinsRejectsZeroDelta(t *testing.T) {
	h, _, _ := newTestHandler(t)
	w := postJSON(t, h.Router(), "/api/coins", `{"target":"alice","delta":0}`, "s3cret")
	if w.Code != http.StatusBadRequest {
		t.Fatalf("delta=0 = %d，期望 400", w.Code)
	}
}

func TestCoinsUpstreamFailure(t *testing.T) {
	h, coins, _ := newTestHandler(t)
	coins.err = errors.New("rpc down")
	w := postJSON(t, h.Router(), "/api/coins", `{"target":"alice","delta":5}`, "s3cret")
	if w.Code != http.StatusServiceUnavailable {
		t.Fatalf("上游失败 = %d，期望 503", w.Code)
	}
}

func TestBotsRejectsMissingKey(t *testing.T) {
	h, _, bots := newTestHandler(t)
	w := postJSON(t, h.Router(), "/api/bots", `{"count":1}`, "")
	if w.Code != http.StatusUnauthorized {
		t.Fatalf("缺密钥 = %d，期望 401", w.Code)
	}
	if bots.gotCount != 0 {
		t.Fatal("鉴权失败的请求不该触达下游")
	}
}

func TestBotsAddsRequestedCount(t *testing.T) {
	h, _, bots := newTestHandler(t)
	w := postJSON(t, h.Router(), "/api/bots", `{"count":3}`, "s3cret")
	if w.Code != http.StatusOK {
		t.Fatalf("= %d，body=%s", w.Code, w.Body.String())
	}
	if bots.gotCount != 3 {
		t.Fatalf("下游收到 count=%d，期望 3", bots.gotCount)
	}
}

func TestBotsRejectsNonPositiveCount(t *testing.T) {
	h, _, bots := newTestHandler(t)
	for _, body := range []string{`{"count":0}`, `{"count":-2}`} {
		if w := postJSON(t, h.Router(), "/api/bots", body, "s3cret"); w.Code != http.StatusBadRequest {
			t.Fatalf("%s = %d，期望 400", body, w.Code)
		}
	}
	if bots.gotCount != 0 {
		t.Fatal("非法 count 不该调下游")
	}
}

func TestBotsUpstreamFailure(t *testing.T) {
	h, _, bots := newTestHandler(t)
	bots.err = errors.New("rpc down")
	w := postJSON(t, h.Router(), "/api/bots", `{"count":1}`, "s3cret")
	if w.Code != http.StatusServiceUnavailable {
		t.Fatalf("上游失败 = %d，期望 503", w.Code)
	}
}
```

- [ ] **Step 4: 跑测试确认失败**

```powershell
go test -count=1 ./gm
```

预期：编译失败，`undefined: NewHandler`。

- [ ] **Step 5: 实现 Gin 服务**

```go
// joltgo/gm/server.go
// Package gm 是 GM 服务：一个只对运维可见的 HTTP 入口（Gin）+ 内嵌 Web 操作页。
//
// 它不进玩家协议：gate 的路由表里没有 gm.*，它自己也不监听任何 pitaya 端口。
// 它只做两件事 —— 校验管理密钥，然后把指令转成后端 RPC 交给拥有该状态的
// 服务去执行（match 管队列、logic 管钱包）。因此本包不碰 match:queue 的键名，
// 也不碰钱包 Hash。
package gm

import (
	"context"
	"crypto/subtle"
	"errors"
	"log"
	"net/http"
	"strconv"
	"strings"

	"github.com/gin-gonic/gin"
	"joltgo/persist"
)

// CoinsService 是「加/扣金币」这一个能力；由 gm.RPC 实现（转成后端 RPC 调 logic）。
// 定义成窄接口而不是直接调 RPC：HTTP 层因此可以用 stub 测，不用起集群。
type CoinsService interface {
	GrantCoins(ctx context.Context, accountID string, delta int64) (int64, error)
}

// AddBotsService 是「往匹配队列塞机器人」这一个能力；由 gm.RPC 实现。
//
// 注意它接的是**业务参数**（数量），不是 proto 消息：密钥由 gm.RPC 在构造时固定、
// 由它注入到 RPC 请求里，HTTP 层根本不需要知道有 admin_key 这回事。
type AddBotsService interface {
	AddBots(ctx context.Context, count int32) (int32, error)
}

// Handler 持有 GM 页面的依赖。accounts 用于「用户名 → accountID」解析。
type Handler struct {
	coins    CoinsService
	bots     AddBotsService
	accounts *persist.AccountStore
	secret   string
	page     []byte
}

// NewHandler 构造 GM 处理器。secret 为空时所有 /api/* 都返回 401。
func NewHandler(coins CoinsService, bots AddBotsService, accounts *persist.AccountStore, secret string) *Handler {
	return &Handler{coins: coins, bots: bots, accounts: accounts, secret: secret, page: indexPage}
}

// Router 组装 Gin 路由。页面放行（不含数据），/api/* 一律过鉴权中间件。
func (h *Handler) Router() *gin.Engine {
	r := gin.New()
	r.Use(gin.Recovery())
	r.GET("/", func(c *gin.Context) {
		c.Data(http.StatusOK, "text/html; charset=utf-8", h.page)
	})
	api := r.Group("/api", h.requireKey)
	api.POST("/coins", h.postCoins)
	api.POST("/bots", h.postBots)
	return r
}

// requireKey 校验 Authorization: Bearer <key>，比对走常数时间。
func (h *Handler) requireKey(c *gin.Context) {
	token := strings.TrimPrefix(c.GetHeader("Authorization"), "Bearer ")
	if h.secret == "" || subtle.ConstantTimeCompare([]byte(h.secret), []byte(token)) != 1 {
		c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{"ok": false, "reason": "unauthorized"})
	}
}

// postCoins 处理「给玩家发钱」。
//
// target 是纯十进制就按 accountID 处理，否则按用户名解析 —— 运维手上拿到的通常
// 是玩家吆喝的那串字，而 accountID 在排查时更好用，两条都支持。
func (h *Handler) postCoins(c *gin.Context) {
	var req struct {
		Target string `json:"target"`
		Delta  int64  `json:"delta"`
	}
	if err := c.ShouldBindJSON(&req); err != nil || strings.TrimSpace(req.Target) == "" || req.Delta == 0 {
		c.JSON(http.StatusBadRequest, gin.H{"ok": false, "reason": "bad_request"})
		return
	}

	accountID, err := h.resolveAccountID(c.Request.Context(), strings.TrimSpace(req.Target))
	if err != nil {
		if errors.Is(err, errPlayerNotFound) {
			c.JSON(http.StatusNotFound, gin.H{"ok": false, "reason": "player_not_found"})
			return
		}
		log.Printf("gm: resolve target %q failed: %v", req.Target, err)
		c.JSON(http.StatusServiceUnavailable, gin.H{"ok": false, "reason": "upstream_failed"})
		return
	}

	coins, err := h.coins.GrantCoins(c.Request.Context(), accountID, req.Delta)
	if err != nil {
		log.Printf("gm: grant coins to %s failed: %v", accountID, err)
		c.JSON(http.StatusServiceUnavailable, gin.H{"ok": false, "reason": "upstream_failed"})
		return
	}
	c.JSON(http.StatusOK, gin.H{"ok": true, "account_id": accountID, "coins": coins})
}

// postBots 处理「往匹配队列塞机器人」。
func (h *Handler) postBots(c *gin.Context) {
	var req struct {
		Count int32 `json:"count"`
	}
	if err := c.ShouldBindJSON(&req); err != nil || req.Count <= 0 {
		c.JSON(http.StatusBadRequest, gin.H{"ok": false, "reason": "bad_request"})
		return
	}
	enqueued, err := h.bots.AddBots(c.Request.Context(), req.Count)
	if err != nil {
		log.Printf("gm: add bots failed: %v", err)
		c.JSON(http.StatusServiceUnavailable, gin.H{"ok": false, "reason": "upstream_failed"})
		return
	}
	c.JSON(http.StatusOK, gin.H{"ok": true, "enqueued": enqueued})
}

var errPlayerNotFound = errors.New("player not found")

// resolveAccountID 把页面上的目标解析成 accountID：纯十进制直接用，否则按用户名查。
//
// 用户名必须走 account.NormalizeUsername（注册时用的就是它，用户名大小写不敏感），
// 自己拼 "acct:name:" 会在「页面填 Alice、注册的是 alice」时查不到。
func (h *Handler) resolveAccountID(ctx context.Context, target string) (string, error) {
	if _, err := strconv.ParseUint(target, 10, 64); err == nil {
		return target, nil
	}
	id, found, err := h.accounts.LookupByUsername(ctx, target)
	if err != nil {
		return "", err
	}
	if !found {
		return "", errPlayerNotFound
	}
	return id, nil
}
```

`persist.AccountStore` 上若还没有 `LookupByUsername(ctx, name) (string, bool, error)`，就在 `persist/store.go` 加一个薄封装：用 `account.NormalizeUsername` 规范化后 `GET acct:name:<name>`，返回十进制 id 字符串。**注意不要 import `account` 包**（会形成环）—— 规范化函数若在 `account` 包里，就把它的实现（小写化）在 `persist` 里复制一份，并在两处都写明「与 account.NormalizeUsername 必须一致」；更干净的做法是把 `NormalizeUsername` 下沉到 `persist`（或一个新的极小包）后由 `account` 引用，实现时二选一并保证只有一份真相。

- [ ] **Step 6: 给页面一个最小占位（Task 9 换成真页面）**

```go
// joltgo/gm/page.go
package gm

import _ "embed"

//go:embed index.html
var indexPage []byte
```

```html
<!-- joltgo/gm/index.html -->
<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><title>GM</title></head>
<body><p>GM 页面占位（Task 9 实现）</p></body></html>
```

- [ ] **Step 7: 跑测试确认通过**

```powershell
go test -count=1 ./gm
```

预期：全绿。

- [ ] **Step 8: 提交**

```powershell
git add joltgo/gm joltgo/go.mod joltgo/go.sum joltgo/persist
git commit -m "feat: 新增 gm 服务的 HTTP 入口与两个接口"
```

---

## Task 9: GM 操作页

**Files:**
- Modify: `joltgo/gm/index.html`
- Test: `joltgo/gm/server_test.go`（补两个页面断言）

**Interfaces:**
- Consumes: `/api/coins`、`/api/bots` 的请求/响应形状（Task 8）
- Produces: 可用的单页界面；无新 Go 符号

- [ ] **Step 1: 写失败的测试**

```go
func TestPageContainsBothFormsAndKeyField(t *testing.T) {
	h, _, _ := newTestHandler(t)
	req := httptest.NewRequest(http.MethodGet, "/", nil)
	w := httptest.NewRecorder()
	h.Router().ServeHTTP(w, req)
	body := w.Body.String()

	for _, want := range []string{
		`id="gmkey"`,   // 密钥输入框
		`id="target"`,  // 发钱目标（用户名或 accountID）
		`id="delta"`,   // 发钱数量
		`id="coinbtn"`, // 发钱按钮
		`id="botcount"`,
		`id="botbtn"`,
		`id="result"`,   // 结果区
		"/api/coins",    // 两个接口都要被页面调用
		"/api/bots",
		"localStorage", // 密钥要能记住
	} {
		if !strings.Contains(body, want) {
			t.Errorf("页面缺少 %s", want)
		}
	}
}

func TestPageSendsBearerHeader(t *testing.T) {
	h, _, _ := newTestHandler(t)
	req := httptest.NewRequest(http.MethodGet, "/", nil)
	w := httptest.NewRecorder()
	h.Router().ServeHTTP(w, req)
	if !strings.Contains(w.Body.String(), "Bearer") {
		t.Error("页面必须用 Authorization: Bearer 发密钥，否则所有请求都会 401")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
go test -count=1 -run TestPage ./gm
```

预期：FAIL（占位页面没有这些元素）。

- [ ] **Step 3: 写页面**

```html
<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>GM 控制台</title>
<style>
:root { --bg:#14181d; --panel:#1c2229; --line:#2b333d; --fg:#e6edf3; --dim:#8b98a5; --accent:#4aa3ff; --bad:#ff6b6b; --good:#5ddc7a; }
* { box-sizing:border-box; }
body { margin:0; padding:24px; background:var(--bg); color:var(--fg);
  font:14px/1.6 system-ui,"Segoe UI",sans-serif; }
h1 { font-size:18px; margin:0 0 16px; }
.panel { background:var(--panel); border:1px solid var(--line); border-radius:8px;
  padding:16px; margin-bottom:16px; max-width:640px; }
.panel h2 { font-size:14px; margin:0 0 12px; color:var(--dim); font-weight:600; }
label { display:block; font-size:12px; color:var(--dim); margin:8px 0 4px; }
input { width:100%; padding:8px 10px; background:#0f1317; color:var(--fg);
  border:1px solid var(--line); border-radius:6px; font:inherit; }
button { margin-top:12px; padding:9px 18px; background:var(--accent); color:#06121f;
  border:0; border-radius:6px; font:inherit; font-weight:600; cursor:pointer; }
#result { margin-top:16px; padding:12px; border-radius:6px; border:1px solid var(--line);
  background:#0f1317; max-width:640px; white-space:pre-wrap; min-height:44px; }
.ok { color:var(--good); } .bad { color:var(--bad); }
.hint { color:var(--dim); font-size:12px; margin-top:8px; }
</style>
</head>
<body>
<h1>GM 控制台</h1>

<div class="panel">
  <h2>管理密钥</h2>
  <label for="gmkey">密钥（-gmkey / GM_KEY，仅保存在本机浏览器）</label>
  <input id="gmkey" type="password" autocomplete="off" placeholder="与服务端 -gmkey 一致">
</div>

<div class="panel">
  <h2>发钱</h2>
  <label for="target">目标（用户名或 accountID）</label>
  <input id="target" placeholder="alice 或 10001">
  <label for="delta">数量（负数表示扣除）</label>
  <input id="delta" type="number" value="500">
  <button id="coinbtn" type="button">发送</button>
</div>

<div class="panel">
  <h2>机器人</h2>
  <label for="botcount">数量（单次上限 8）</label>
  <input id="botcount" type="number" value="1" min="1" max="8">
  <button id="botbtn" type="button">加入匹配队列</button>
  <p class="hint">机器人会与下一位入队的真人配对：进对局后全程不动、不开火，被打死原地复活。</p>
</div>

<div id="result">等待操作…</div>

<script>
const keyEl = document.getElementById('gmkey');
const outEl = document.getElementById('result');

keyEl.value = localStorage.getItem('gmkey') || '';
keyEl.addEventListener('change', () => localStorage.setItem('gmkey', keyEl.value));

function show(ok, text) {
  outEl.className = ok ? 'ok' : 'bad';
  outEl.textContent = text;
}

async function call(path, payload) {
  const key = keyEl.value.trim();
  if (!key) { show(false, '请先填管理密钥'); return null; }
  localStorage.setItem('gmkey', key);
  try {
    const resp = await fetch(path, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + key },
      body: JSON.stringify(payload),
    });
    const body = await resp.json().catch(() => ({}));
    if (!resp.ok || body.ok === false) {
      show(false, '失败：' + (body.reason || resp.status));
      return null;
    }
    return body;
  } catch (err) {
    show(false, '请求失败：' + err);
    return null;
  }
}

document.getElementById('coinbtn').addEventListener('click', async () => {
  const target = document.getElementById('target').value.trim();
  const delta = parseInt(document.getElementById('delta').value, 10) || 0;
  if (!target || delta === 0) { show(false, '请填目标和非零数量'); return; }
  const body = await call('/api/coins', { target, delta });
  if (body) {
    show(true, target + '（' + body.account_id + '）余额 ' + body.coins);
  }
});

document.getElementById('botbtn').addEventListener('click', async () => {
  const count = parseInt(document.getElementById('botcount').value, 10) || 0;
  if (count <= 0) { show(false, '数量必须大于 0'); return; }
  const body = await call('/api/bots', { count });
  if (body) {
    show(true, '已入队 ' + body.enqueued + ' 个机器人，等待真人入队配对。');
  }
});
</script>
</body>
</html>
```

- [ ] **Step 4: 跑测试确认通过并提交**

```powershell
go test -count=1 ./gm
git add joltgo/gm
git commit -m "feat: GM 操作页"
```

预期：全绿。

---

## Task 10: `main.go` 装配第六个角色 + deploy 脚本

**Files:**
- Modify: `joltgo/main.go`
- Create: `joltgo/gm/rpc.go`
- Modify: `joltgo/deploy/start-all.ps1`
- Modify: `joltgo/README.md`（若里面列了角色清单）

**Interfaces:**
- Consumes: `gm.NewHandler`、`match.New(..., secret)`、`logic.NewComponentWithSecret`（Task 4 / 7 / 8）
- Produces: `-type gm` 可用；`-gmaddr` / `-gmkey` 两个 flag；`gm.RPC` / `gm.NewRPC`

- [ ] **Step 1: 加 flag 与启动校验**

```go
	svType := flag.String("type", "gate", "server type: gate | account | logic | match | game | gm")
	redisAddr := flag.String("redis", kv.DefaultAddr, "redis address (host:port)")
	gmAddr := flag.String("gmaddr", ":8082", "gm http listen address (type=gm)")
	gmKey := flag.String("gmkey", "", "gm admin key (type=gm, account|logic|match); falls back to $GM_KEY")
	flag.Parse()

	secret := *gmKey
	if secret == "" {
		secret = os.Getenv("GM_KEY")
	}
	if *svType == "gm" && secret == "" {
		// 默认拒绝服务比默认开放安全：忘记配置的后果是服务起不来，
		// 而不是「谁都能发指令」。
		log.Fatalf("-type gm 需要 -gmkey 或 GM_KEY：没配密钥就不该监听管理端口")
	}
```

把 `secret` 与 `*gmAddr` 一并传进 `run`（现在的调用是 `run(svType, builder, *redisAddr)`，改成 `run(svType, builder, *redisAddr, *gmAddr, secret)`，`run` 的签名同步改）。

- [ ] **Step 2: 加 `case "gm"` 分支**

```go
	case "gm":
		// gm 是 backend：注册进 etcd 只为「主动发后端 RPC」，不注册任何 handler /
		// remote，也不加 acceptor —— 它没有任何 pitaya 监听端口，管理流量走 Gin。
		//
		// 它需要 Redis 是为了「用户名 → accountID」（只读账号 Hash，不写）。
		accountPool, err := kv.OpenRedigo(context.Background(), redisAddr)
		if err != nil {
			return err
		}
		defer accountPool.Close()

		rpc := gm.NewRPC(app, secret)
		handler := gm.NewHandler(rpc, rpc, persist.NewAccountStore(accountPool), secret)
		go func() {
			// Run 是阻塞的：放进 goroutine，让 app.Start() 继续处理信号与优雅退出。
			if err := handler.Router().Run(gmAddr); err != nil {
				log.Fatalf("gm http 启动失败: %v", err)
			}
		}()
		log.Printf("gm: http 监听 %s", gmAddr)
```

- [ ] **Step 3: 把 secret 传给 logic 与 match**

`logic` 分支里 **handler 与 remote 必须是同一个实例**（`GrantCoins` 要能被 `RPCTo` 调到，密钥状态也只有一份）：

```go
		comp := logic.NewComponentWithSecret(app, service, secret)
		app.Register(comp, component.WithName("logic"), component.WithNameFunc(strings.ToLower))
		app.RegisterRemote(comp, component.WithName("logic"), component.WithNameFunc(strings.ToLower))
```

`match` 分支只把 `match.New(app, match.NewQueue(rdb), online.NewStore(rdb))` 改成
`match.New(app, match.NewQueue(rdb), online.NewStore(rdb), secret)`，并补一行 `app.RegisterRemote(...)`：

```go
	matcher := match.New(app, match.NewQueue(rdb), online.NewStore(rdb), secret)
	app.Register(matcher, component.WithName("match"), component.WithNameFunc(strings.ToLower))
	app.RegisterRemote(matcher, component.WithName("match"), component.WithNameFunc(strings.ToLower))
```

- [ ] **Step 4: 实现 `gm` 的 RPC 客户端**

```go
// joltgo/gm/rpc.go
package gm

import (
	"context"
	"errors"
	"log"

	pitaya "github.com/topfreegames/pitaya/v3/pkg"
	"joltgo/game/protos"
)

// 服务类型名与 route 名：前者与各角色启动时的 serverType 一致，
// 后者是三段式 route（server.service.method）。
const (
	logicServerType = "logic"
	matchServerType = "match"

	grantCoinsRoute = "logic.logic.grantcoins"
	addBotsRoute    = "match.match.addbots"
)

// ErrNoServer 表示集群里没有该类型的节点（服务没起、或刚重启）。
var ErrNoServer = errors.New("gm: no server available")

// RPC 是 gm 到后端的最小客户端：两个能力各一条 RPC。
//
// 它只发不收 —— gm 不注册 handler / remote，因此不存在「客户端能调到 gm」这件事。
// 密钥在构造时固定，由它注入到每条请求里（HTTP 层因此不需要知道 admin_key 的存在）。
//
// 能不能发 app.RPCTo 只取决于「这个进程有没有以 pitaya.Cluster 模式构建 app」，
// 与 frontend / backend 无关：gm 是 backend，照样能主动调别的后端。
type RPC struct {
	app    pitaya.Pitaya
	secret string
}

// NewRPC 构造 RPC 客户端。
func NewRPC(app pitaya.Pitaya, secret string) *RPC {
	return &RPC{app: app, secret: secret}
}

// GrantCoins 转给任意一个 logic 节点。
func (r *RPC) GrantCoins(ctx context.Context, accountID string, delta int64) (int64, error) {
	serverID, err := r.pick(logicServerType)
	if err != nil {
		return 0, err
	}
	reply := &protos.GrantCoinsReply{}
	if err := r.app.RPCTo(ctx, serverID, grantCoinsRoute, reply, &protos.GrantCoinsMsg{
		AccountId: accountID,
		Delta:     delta,
		AdminKey:  r.secret,
	}); err != nil {
		return 0, err
	}
	if !reply.GetOk() {
		return 0, errors.New("gm: grant coins rejected: " + reply.GetReason())
	}
	return reply.GetCoins(), nil
}

// AddBots 转给任意一个 match 节点，返回实际入队数量。
func (r *RPC) AddBots(ctx context.Context, count int32) (int32, error) {
	serverID, err := r.pick(matchServerType)
	if err != nil {
		return 0, err
	}
	reply := &protos.AddBotsReply{}
	if err := r.app.RPCTo(ctx, serverID, addBotsRoute, reply, &protos.AddBotsMsg{
		Count:    count,
		AdminKey: r.secret,
	}); err != nil {
		return 0, err
	}
	if !reply.GetOk() {
		return 0, errors.New("gm: add bots rejected: " + reply.GetReason())
	}
	return reply.GetEnqueued(), nil
}

// pick 从服务发现里取任意一个该类型的节点。这些服务都无状态（状态在 Redis），
// 挑谁都一样 —— 与 match.startMatch 选 game 节点的做法一致。
func (r *RPC) pick(serverType string) (string, error) {
	servers, err := r.app.GetServersByType(serverType)
	if err != nil {
		log.Printf("gm: discover %s failed: %v", serverType, err)
		return "", err
	}
	for id := range servers {
		return id, nil
	}
	return "", ErrNoServer
}

// 确保 *RPC 满足 Handler 依赖的两个窄接口。
var (
	_ CoinsService   = (*RPC)(nil)
	_ AddBotsService = (*RPC)(nil)
)
```

- [ ] **Step 5: 编译并跑全量单测**

```powershell
go build ./...
go test -count=1 ./bot ./game ./match ./logic ./persist ./gm
```

预期：编译通过、全绿。

- [ ] **Step 6: 改 deploy 脚本**

`joltgo/deploy/start-all.ps1` 开头取一次密钥（可用参数覆盖），并在五进程之后加第六行：

```powershell
param(
    [string]$gmKey = $env:GM_KEY
)
if (-not $gmKey) { $gmKey = 'local-dev-key' }

Start-Process -FilePath $exe -ArgumentList @('-type', 'gm', '-gmkey', $gmKey) -WorkingDirectory (Split-Path $exe) -RedirectStandardOutput (Join-Path $root 'gm.out.log') -RedirectStandardError (Join-Path $root 'gm.log') -WindowStyle Hidden
```

结尾提示改成：

```powershell
Write-Host "started gate (ws://localhost:8080) + account + logic + match + game + gm (http://localhost:8082)"
```

同时 grep 一遍 `五` / `五进程`，把 `start-infra.ps1`、`stop-infra.ps1`、`deploy/README.md` 里过期的描述一起改掉（`stop-infra.ps1` 的进程匹配要覆盖 `-type gm` 那条命令行）。

- [ ] **Step 7: 冒烟：起真集群，真调一次**

```powershell
cd joltgo; .\build.ps1
cd deploy; .\stop-infra.ps1; .\start-all.ps1
Start-Sleep -Seconds 5
Invoke-RestMethod -Uri http://localhost:8082/api/bots -Method Post -Headers @{ Authorization = 'Bearer local-dev-key' } -ContentType 'application/json' -Body '{"count":1}'
Get-Content .\match.log -Tail 5
```

预期：返回 `ok / enqueued=1`，`match.log` 出现 `match: enqueued 1 bots (requested 1)`；用错密钥再试应得 401，且 `match.log` 没有新的入队日志。

- [ ] **Step 8: 提交**

```powershell
git add joltgo/main.go joltgo/gm joltgo/deploy joltgo/README.md
git commit -m "feat: 启动第六个角色 gm（HTTP 管理入口）"
```

---

## Task 11: 文档同步

**Files:**
- Modify: `AGENTS.md`
- Modify: `docs/ARCHITECTURE.md`、`docs/API.md`、`docs/BUILD.md`、`docs/DEVELOPMENT.md`
- Modify: `joltgo/deploy/README.md`

**Interfaces:**
- Consumes: 前十个任务的最终实现（角色名、flag 名、route 名、错误码）
- Produces: 无代码

- [ ] **Step 1: `AGENTS.md`**

§1 定位句里「五个角色」改六角色并补 `gm` 职责；§2 目录树加 `gm/` 与 `bot/`；§5 加两条约定：

```markdown
- **GM 指令不进玩家协议**：`gm` 是第六个角色（backend），自己起 Gin HTTP 端口 + 内嵌 Web 操作页，
  gate 的路由表里没有 `gm.*`，它也不监听任何 pitaya 端口、不注册 handler/remote。
  它只**主动**发后端 RPC（`match.match.addbots` / `logic.logic.grantcoins`），两条 route 都要求
  「调用方没有会话」+ 共享密钥（`-gmkey` / `GM_KEY`）—— 前者挡客户端经 gate 发来的调用
  （route 名不是权限），后者挡其它后端。密钥为空 = 不提供管理入口。GM 操作**不审计**，
  只写 `deploy/gm.log`。
- **机器人是队列里的普通成员**：uid 前缀 `bot:`（唯一真相在 `joltgo/bot/`），由 `match` 自己
  入队（`gm` 不碰 `match:queue`）。配对时机器人跳过「读在线登记 + 请 gate 写会话数据」；
  全机器人配对直接丢弃且不放回；对局内它是不收帧、不进注册表的「无客户端槽位」，
  结算靠既有的「空槽位」规则跳过 —— logic 不需要认识机器人。
```

- [ ] **Step 2: `docs/ARCHITECTURE.md`**：六角色拓扑图加 `gm`（左侧 HTTP、右侧 RPC 到 match/logic），补一节「GM 与机器人」，把 spec §4 / §7 的结论写全。特别写进这条技术前提：**能不能发 `app.RPCTo` 取决于进程有没有以 `pitaya.Cluster` 模式构建 app，与 frontend/backend 无关**；`pkg/client/client.go` 是 acceptor 客户端，发不出后端 RPC。这是本次最容易再次踩到的认知坑。

- [ ] **Step 3: `docs/API.md`**：补两节 ——
  - **GM HTTP 接口**：`GET /`、`POST /api/coins`、`POST /api/bots` 的请求/响应字段、`Authorization: Bearer <gmkey>`、状态码表（400 / 401 / 404 / 409 / 503）。
  - **服务间 route**：`match.match.addbots`（`AddBotsMsg` / `AddBotsReply`）与 `logic.logic.grantcoins`（`GrantCoinsMsg` / `GrantCoinsReply`），写明两道入口检查与原因码（`forbidden` / `account_not_found` / `player_not_found` / `noop` / `internal`）。

- [ ] **Step 4: `docs/BUILD.md`**：新增依赖小节写 Gin v1.12.0（纯 Go，不影响 UCRT64 / cgo 构建链）；若 Task 2 用了新的 `protoc` 命令，把命令原样记进 `game.proto` 的生成说明，并注明「持久化模型走 `gen-redis.ps1`，两者不要混用」。

- [ ] **Step 5: `docs/DEVELOPMENT.md`**：加一节「用 GM 页面发钱 / 加机器人」——六个进程怎么起、`-gmkey` 怎么配、页面地址（`http://localhost:8082`）、以及「加 2 个机器人不会互打（会被丢弃），要等真人入队」。

- [ ] **Step 6: `joltgo/deploy/README.md`**：进程清单加 `gm`、日志文件加 `gm.log`、`-gmkey` 的配置方式与「没配就不启动」。

- [ ] **Step 7: 提交**

```powershell
git add AGENTS.md docs joltgo/deploy/README.md
git commit -m "docs: 同步 GM 服务与机器人约定"
```

---

## Task 12: 端到端验收

**Files:** 无（只跑验证；发现问题回到对应任务修）

**Interfaces:**
- Consumes: 全部
- Produces: 一次可复现的验收记录（写进提交信息或任务描述，不落新文档）

- [ ] **Step 1: 起六进程**

```powershell
cd joltgo; .\build.ps1
cd deploy; .\stop-infra.ps1; .\start-all.ps1
Start-Sleep -Seconds 5
Get-Content .\gm.log -Tail 5
```

预期：`gm.log` 有 HTTP 监听日志、无 `启动失败`。

- [ ] **Step 2: 发钱（用户名路径）**

先用真实客户端注册账号 `gmtest`，再：

```powershell
Invoke-RestMethod -Uri http://localhost:8082/api/coins -Method Post -Headers @{ Authorization = 'Bearer local-dev-key' } -ContentType 'application/json' -Body '{"target":"gmtest","delta":500}'
```

预期：`ok=true`、`coins=1500`；客户端进商城看到 1500（不是初始的 1000）。

- [ ] **Step 3: accountID 路径等价**

用上一步返回的 `account_id` 再发 1 个金币，预期 `coins=1501`。若两次落到不同账号，回 Task 8 的 `resolveAccountID` 修。

- [ ] **Step 4: 加机器人 + 真人匹配**

```powershell
Invoke-RestMethod -Uri http://localhost:8082/api/bots -Method Post -Headers @{ Authorization = 'Bearer local-dev-key' } -ContentType 'application/json' -Body '{"count":1}'
```

随后客户端点「开始匹配」。预期：秒配上；对面站着一个不动的对手；打它掉血；打到 10 杀结束这局；结果面板正常；对方被击杀后在出生点满血复活。

- [ ] **Step 5: 结算不落脏数据**

打完这局后看个人信息页：场次 / 胜 / 击杀正常累加；最近对局里对手名字**不是** `bot:xxx`。

```powershell
cd deploy; .\redis-cli.exe LRANGE playerhist:<accountID> 0 0
```

预期：对手名字为空串（机器人槽位是空 uid，被 `RecordMatch` 跳过）。

- [ ] **Step 6: 全机器人配对被丢弃**

```powershell
Invoke-RestMethod -Uri http://localhost:8082/api/bots -Method Post -Headers @{ Authorization = 'Bearer local-dev-key' } -ContentType 'application/json' -Body '{"count":2}'
Start-Sleep -Seconds 2
Get-Content .\match.log -Tail 10
```

预期：出现 `discarding all-bot pair`，且**不会**有该 matchId 的 `created instance` 日志。

- [ ] **Step 7: 权限负路径**

```powershell
# 缺密钥
Invoke-RestMethod -Uri http://localhost:8082/api/bots -Method Post -ContentType 'application/json' -Body '{"count":1}'
# 错密钥
Invoke-RestMethod -Uri http://localhost:8082/api/bots -Method Post -Headers @{ Authorization = 'Bearer wrong' } -ContentType 'application/json' -Body '{"count":1}'
```

预期：两次都是 401，且 `match.log` 里没有对应的入队日志。

- [ ] **Step 8: 客户端直调管理 route 被拒**

用客户端会话发 `match.match.addbots`（带 `admin_key`），预期 `match.log` 出现 `addbots from client rejected`、队列不变。这一步验证的是「route 名不是权限」这条设计前提。

- [ ] **Step 9: 全量回归**

```powershell
cd joltgo; go test -count=1 ./...
```

预期：全绿（`./physics` 的 `joltdll` 用例不在默认集合内，按 `docs/DEVELOPMENT.md` 单独跑）。

---

## Self-Review 记录

- **spec 覆盖**：§4 角色与包 → Task 1 / 8 / 10；§5 协议 → Task 2；§6 HTTP 接口与页面 → Task 8 / 9；§7 匹配侧（7.1 入队、7.2 探活、7.3 全机器人丢弃、7.4 状态推送）→ Task 3 / 4 / 5；§8 对局侧 → Task 6；§9 逻辑侧 → Task 7；§10 错误处理 → Task 8 / 10 与 Task 12 Step 7；§11 测试 → 各任务 Step 1 与 Task 12；§12 文档 → Task 11。
- **占位符**：无 TBD / TODO / 「实现细节待定」；每个代码步骤都给了可直接粘贴的实现。
- **类型一致性**：`bot.New` / `bot.Is`、`Queue.QueueBots(ctx,n,newUID)`、`Component.QueueBots(ctx,msg)`、`Service.GrantCoins(ctx,id,delta)`、`Component.GrantCoins(ctx,msg)`、`CoinsService` / `AddBotsService`（HTTP 层用业务参数，不碰 proto）、`gm.NewHandler` / `gm.Router` / `gm.NewRPC` 在全部任务里名字与签名一致；proto 字段 `AddBotsMsg.Count/AdminKey`、`GrantCoinsMsg.AccountId/Delta/AdminKey` 与 Task 2 一致。
- **已知待对齐项**（实现时按仓库实际代码定，不改变设计）：`match_test.go` 里 stub 的字段名（`joinTestApp` / `fakeSession` / `pushRecord` / `singleGameServer`）、`logic/service_test.go` 的 `newLockedServiceTestEnv` 形状、`game/component_test.go` 的 `fakeGameApp` 名字、`game.proto` 的 `protoc` 生成命令、以及「用户名规范化函数该不该从 `account` 下沉到 `persist`」。这五处都写明了「复用既有，不要新建第二套」或「二选一并保证只有一份真相」。
